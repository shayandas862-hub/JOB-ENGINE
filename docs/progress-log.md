# Progress Log — goal-a-engine (Architecture v2 build)

One line per completed task, dated, newest first. Format:
`YYYY-MM-DD HH:MM TZ — Phase N, Task M: what shipped (tests: X passing)`

---

## ⚑ FOUNDER ATTENTION QUEUE — updated 2026-07-11 22:20 BST (compiled while you were in your meeting)

You asked for everything needing your attention in one place. Prioritised by impact on Goal A (a sponsored UK job). Nothing here is urgent-tonight except possibly the applies.

**① APPLY — highest leverage, do this first.** 74 High-fit roles are already in your queue; the 28 strongest (real links, best sponsor evidence) are written up in **`ops/apply-shortlist.md`**. Tier 1 = 3 Anthropic "Applied AI Architect" roles in London (role-confirmed, clears the salary wall). You can apply today with your own CV — the engine never applies for you (by design). Deadlines shown are *estimates*, not hard cutoffs.

**② SEED `cv_blocks` — the single biggest automation unlock.** The table is empty, so no tailored CV can be generated. It needs your real CV turned into confirmed fact-blocks (personal data lives in the DB, never in code, and must be human-confirmed — so this is yours to do). Once seeded, `generate_cv <role_id>` produces a truth-gated tailored CV per role. Until then, apply with your existing CV.

**③ Free API keys (all optional, each degrades cleanly if unset):**
- `COMPANIES_HOUSE_API_KEY` — enriches the running census with official industry codes / active-or-dissolved / company age. Free, ~5 min: register at developer.company-information.service.gov.uk → create an application → copy the REST key into `.env`. **Until set, tonight's census is probe-only (still gathering boards + real job links).**
- `ADZUNA_APP_ID` + `ADZUNA_APP_KEY` (developer.adzuna.com) and `REED_API_KEY` (reed.co.uk/developers) — turn on role-first discovery (search boards by your target roles, not just tracked companies). Free, ~10 min.
- Notion filing — create an integration at notion.so/my-integrations, share a page with it, set `NOTION_TOKEN` + `NOTION_PARENT_PAGE_ID` + `NOTION_DATABASE_ID`. Turns on the auto-filing tracker.

**④ Switches that start real activity (your call):**
- **ntfy channel** — set your notification channel so the phone nudges fire.
- **launchd load** — turns on the automatic daily pipeline (begins real daily Gemini spend). Command in `docs/dev.md`.
- **Phase 8 (Going Live) greenlight** — I re-staged the CLAUDE.md but did NOT begin it (it's outward-facing/irreversible + your standing hold). Say go when you want cloud hosting + the public portfolio repo.
- **Commit hold** — the entire build (Phases 1–7.5) is still uncommitted working-tree, per your standing hold. Tell me when to make the first commit.

**What's running for you right now (no action needed):** the census is looping back-to-back overnight (bounded, resumable, **free** — no Gemini, no paid APIs). It's at ~130+ orgs and climbing toward the 126,342-org register. Halt anytime with `touch .sweep-stop` or just tell me.

**What I did NOT do (respecting your holds):** no commits, no publishing, no cloud deploys, nothing touching your credentials, did not begin Phase 8, did not seed cv_blocks (needs your facts), did not auto-apply (engine never applies).

---

- 2026-08-12 22:30 BST — **Phase 9.5, tasks 0–4 and 6: the machine started describing the person, and three tasks changed shape because measuring came first.** **Task 0** closed five of the six close-audit defects with executing guards, each reproduced red before being fixed: the snapshot predicate now needs two signals that agree, so the documented "bad fix" (`cp ops/flip/public-ci.yml .github/workflows/ci.yml`) no longer buys a green suite over a repo that cannot deploy — reproduced first at exactly the audit's 953/65, and after the fix the same mutation gives 7 failed / 955 passed ([[B-GAE-040]]); the probe cleanup commits its drop, and the leftover schema was removed **by running the repaired test**, leaving `pg_namespace` with zero non-system schemas after a full 1,086-test DB run ([[B-GAE-041]]); the README dead-link guard covers children of scrubbed paths, checked in three directions because a widened matcher is the kind that starts catching what it should not ([[B-GAE-042]]); `.dockerignore` went recursive after a locally built image was measured carrying **246 foreign `.pyc` files**, nine of them compiled at a path this repo has not used for weeks — the guard reads the source path recorded inside each `.pyc`, so it is red in a smuggling image and green in a correct one, rather than being the permanently-red "no .pyc exists" test ([[B-GAE-043]]); and the snapshot verifier now fails shut when its scanner errors, proven by shimming a `grep` that exits 2 onto PATH ([[B-GAE-045]]). **Guardless ratchet 19 → 14**, its first fall since the audit raised it. **Task 1 — the mirror:** `describe_the_owner` (tool 48) and a dashboard card over `v_owner_mirror` (0063), reading the fact base back as understanding, with the uncomfortable half leading. Nothing is stored — the understanding is re-formed on every read and a test asserts the module issues no write of any kind. On the founder's own base: **38 facts, 21 live skills, 4 evidenced, 17 not — 19% coverage**, and nothing had ever told him; the 17 are named rather than counted, because a count is not actionable. **Task 2 (M6) — `amend_cv_block`:** retire-plus-redraft in one audited step, insert before stamp so a failure leaves the old fact serving; the superseded row keeps its wording *and* its confirmed state, and `amended_from` (0064) points backwards only. **Task 3 (M7) — `record_experience`:** the prompt option was not hypothetical, it shipped in Phase 9 and had already failed — 6 role blocks evidencing "teamwork", "call handling", "cinematography", **not one matching any `my_skills` row**, every norm correctly normalised. So one payload now writes both, joined by construction from the one normaliser, and the round-trip test proves a skill written the new way comes back **provable** while one written the old way in the same probe stays unprovable. `intake-v1 → v2`. **Task 4 — the learning curve:** briefed over `my_skills.learned_at` on the stated basis of "weeks of data"; measured **22 skills, 0 with `learned_at`**, every row six weeks older than the column ([[B-GAE-046]]), so ranking on it would have returned an empty list forever from code that reviews as correct. Rebuilt on demand × held × proven in three effort tiers, tier outranking demand — against live: **902 skills in demand, 21 held, 4 closed**, and the top row is "AI agent & pipeline design", **65 roles asking**, held, unproven. **Task 6 — the salary gate:** grounded against `clean_html`-stripped text using the ONE shared stripper; measured before the change, **63 of 212 salary ranges in 334 HTML adverts (30%)** were readable to a human and ungroundable in the raw bytes, role 7090 among them. Skills keep the strict gate, pinned. A rejected salary now **holds** the listing in the tray with its claim instead of releasing it, with dropping the salary always available as the way out. `extract-v1 → v2`. Tools **47 → 51**; migrations through **0064**; measured across all three lanes: offline **1,022 passed / 64 skipped**, DB lane **1,086 passed / 0 skipped**, container **904 / 126**. Database **323 MB of 500**, `profiles` = **1** — neither retention trigger fired, and two of the plan's three options measured as nearly worthless (see [[D-GAE-095]]). **Applications sent: 0.**
- 2026-08-12 18:33 BST — **BOTH PUSH GATES PRESSED, on the founder's explicit order, and the public repo is GREEN for the first time.** The close audit ran first (six defects logged, B-GAE-040–045; B-GAE-044's wrong timestamps corrected in place before anything shipped). Then: private repo pushed `5141005..43a51b0` (14 commits). The snapshot was REBUILT from the corrected tree — `prepare-snapshot.sh` verdict SNAPSHOT CLEAN, suite inside it 953 passed / 65 skipped exit 0, plus an independent by-value scan of every `.env` secret against the snapshot commit: zero hits — then force-pushed to `github.com/shayandas862-hub/JOB-ENGINE-` (`b5ef52e...034bf7b`). Its first Actions run on the two-lane allowlist workflow, **run 31623076331, completed SUCCESS on both jobs** — the offline suite AND the never-before-run database lane (schema built on the runner from genesis + all 62 migrations). **[[B-GAE-039]]'s 404 and [[B-GAE-035]]'s red X are both gone from public; 035 moved to Resolved on seeing the green.** The proof-run button was not pressed first: this machine has no `gh`, the founder ordered the push directly, and the audit had already proven both lanes locally end-to-end — recorded as the deviation it is, not hidden. Applications: still 0.
- 2026-08-12 17:10 BST — **PHASE 9 COMPLETE — a stranger can onboard and never see another user's rows; the phase closed with the 8-step relay.** All seven tasks shipped: friend-tier keys resolving an owner from the token (task 1), RLS policies on all 28 tables proven **by attempting cross-user reads and being refused** and then cut over so the MCP door runs as `goal_a_app` rather than a role that bypasses them (task 2a/2b), the night split into world work once and a personal pass per owner (task 3), onboarding by conversation with the served `intake-v1` interview (task 4), per-owner budgets over shared quotas (task 5), the stranger tier built and deliberately switched OFF at the founder's gate (task 6), and the operator runbook (task 7). **Measured at the close, 2026-08-12 — all four lanes run, not assumed:** offline **960 passed / 58 skipped**; with `RUN_DB_TESTS=1` against the live database **1,018 passed / 0 skipped** (every test in the suite, none skipped); inside the container **900 passed / 118 skipped** plus the unknown-door contract (exit 2); inside the public snapshot **953 passed / 65 skipped** — exit 0 in all four. `ops/ci/verify-genesis.py` **identical across all nine dimensions** (491 columns · 74 constraints · 78 indexes · 30 policies · 12 view names · 12 view bodies · 12 reloptions · 30 RLS tables · 7 triggers), which also exercises `ops/ci/apply-schema.sh` end to end. Migrations through **0062** · **47 MCP tools** · owners **1** · signed-in users **0** · live friend keys **0** · listings **13,049** · companies tracked **894**, of which **91** carry a live board feed · open review flags **1,132** with `promotion_review` still **FULL at 20/20**, so `promote` continues to report `cap_hit` nightly — a founder decision, surfaced not coded around. **Applications sent: 0.** That is still the only number that ultimately matters, and the machine now has to move it for more than one person. The close itself found four more bugs (**B-GAE-036, 037, 038, 039**), every one of them the same shape — a copy that stopped describing what it copies — and three of the four were found by *rebuilding from the copy instead of trusting it*, which before this phase nobody had ever done. *(Time corrected at the close audit: this line first shipped stamped 23:30, hours after the 16:23 commit that wrote it — [[B-GAE-044]].)*

- 2026-08-12 19:40 BST — **Phase 9, task 6: the stranger tier BUILT and switched OFF — B-GAE-032 closed first, then a Google identity becomes an owner.** The task's own ordering was the point: the hole had to close before "authenticated" could mean anybody. **Migration 0061** revokes INSERT/UPDATE/DELETE/TRUNCATE/REFERENCES/TRIGGER from `anon`, `authenticated` and PUBLIC on all **42 relations** in `public` (the bug counted 252 write grants; re-measuring at fix time found **504**, because TRUNCATE/REFERENCES/TRIGGER were never counted), drops the two genesis `FOR ALL` policies and — as a decision, not tidying — the two `anon` SELECT policies, then sets `ALTER DEFAULT PRIVILEGES` so the next table created does not hand it all back. Verified safe first: no repo file names the data API, and neither Cloud Run service carries the publishable key. `tests/test_data_api_lockdown.py` asserts over CLASSES not lists (every relation, every policy, the default ACL, plus RLS-on-everywhere, each with a control); the first three were seen red against the live database. The `supabase_admin` default ACL is unreachable from `postgres` — measured, stated in the migration and the test rather than left silent. **Migration 0062** adds `profiles.auth_user_id` (nullable, unique, no FK to `auth.users`); `src/auth/signin.py` maps a verified `sub` to a profile and creates it on first sight, absorbing a two-request race through a savepoint. `BearerVerifier` gains the JWT path — issuer, audience and ES256 pinned from the project's own live JWKS — and the order stays minted key → JWT → bootstrap, with a failed JWT still falling through so the founder's token can never be locked out by its shape. Six refusals each have their own red-first test with real P-256 signatures (expired, wrong issuer, wrong audience, unsigned, forged, no-issuer-configured). Tools **45 → 47**: `issue_my_key`/`revoke_my_key`, signed-in callers only, no owner argument in either; the old "minting is unreachable from the skin" invariant was narrowed deliberately and its replacement seen red against a mutated import. Secret allowlist **7 → 8** (`SUPABASE_URL`, config not credential — it carries the ref). **Nothing was deployed, nothing pushed, and the Google provider is NOT enabled** — `docs/runbook.md` §7 is the founder's click-path. Measured and reported rather than improvised: the connector's one-click OAuth flow cannot work yet because the project answers `/.well-known/oauth-authorization-server` with **404 "OAuth server is disabled"**. **B-GAE-034** logged (open, unfixed): a discovery stage holds one transaction open across its network fetches — it blocked migration 0062 for ten minutes and will refuse any migration run during a pass. Both lanes green: **1003 passed** with `RUN_DB_TESTS=1` (1003 collected), 945 passed / 58 skipped offline, 900 passed / 103 skipped in the container.

- 2026-08-12 04:01 BST — **Phase 9, task 7: the operator runbook — `docs/runbook.md`.** A non-coder's handbook for running the machine with other people on it: adding a user (`create_profile` then minting a key by conversation), listing/revoking/rotating keys, reading the machine (including why per-owner run lines are numbered and not named), the budget caps and the honest Adzuna question, an incident playbook that says plainly that **a budget stop is not an incident**, and the hard rules — the sacred 06:30 lane, the push gates, and stamps-never-deletes. Built by one session and verified by another: scrub clean (no project ref, no topic, no key) and the suite green. Reading it for the operator flows is what found **B-GAE-033** — `docs/dev.md` frozen at Phase 7.5, stating the tool count twice with one copy stale, a Status section still claiming 351 tests and 19 tools and "nothing committed yet", and a `GEMINI_API_KEY` line contradicting the standing no-AI invariant. The one number that belonged to that session was fixed on sight; the prose rewrite was logged open and carried to the phase close rather than done blind at 4am.

- 2026-08-12 03:48 BST — **Phase 9, task 5: one ledger, two scopes — and the cap moved into the HTTP client.** Per-owner budgets over the shared Adzuna/Reed/Companies House quotas, generalised from the drip's 950/day Reed ledger. A user-triggered call debits BOTH the owner's daily budget and the world cap; the nightly world half passes no owner and debits only the world, so the founder's night is unchanged **by construction and by test**, not by assertion. The gate lives in the two functions that actually reach a metered API — `discover.aggregators._get_json` and `discover.companies_house._get_json` — so a call is metered by **where it points**, and every tool written later inherits it. That is the exact hole **B-GAE-031** came through: Companies House, the largest spender in the system (up to 4,000 calls a night), had **zero ledger rows and always had**, because the ledger arrived with the aggregator layer and the registry client predates it. Nothing had ever failed, which is why nobody looked. Migration **0060**: `api_budget_caps` (editable without a deploy; **no row means no budget and the gate fails closed**) and `api_owner_spend`. Through the MCP door all three ledger tables are **read-only** — an owner sees their own spend and nobody else's, and a forged write raises 42501 — which also closes **B-GAE-030**: `api_quota_ledger` had been writable through the door since 0053, and zeroing the counter the cap is read from would have handed any key holder an unlimited day. The revoke matters as much as the policy, because an UPDATE matching no visible row is 0 rows and no error. A budget refusal is **a stop with receipts, never an item error**: the drip, the registry batch and BOTH ad sweeps catch `BudgetExhausted` above their per-item `except Exception`, where one exhausted day would otherwise have become 200 "broken jobs" or 2,000 fabricated census errors — and the fourth runner was found only by walking every caller of `enrich_org` a second time, after believing the fix was finished. Measured rather than estimated at the founder's push: the discover stage spends **49** unledgered calls (49 role patterns, one per source), and Adzuna on a day that also runs `agg_sweep` totals **289 against a 250 free tier** — so those days were probably already over it, invisibly. The cap stays at the documented 250 with the choice written down rather than raised to hide it (**D-GAE-060**, the founder's call). **S-8 accepted rather than fixed, with its trigger written down**: the MCP rate limiter is in-process against `--max-instances 2`, so it already overshoots 2×. Measured: **915 passed / 50 skipped** offline, **965** with `RUN_DB_TESTS=1`.

- 2026-08-12 02:44 BST — **Phase 9, task 4: onboarding by conversation — the served interview, the profile door, and where typing stops.** `intake-v1` joins `extract-v1` and `cv-v1` as the third versioned, server-side prompt (0013 §6 **M1**), so a new owner's fact base is interviewed to the engine's standard whatever AI they bring — its shape test-pinned in lockstep with `cv.blocks.BLOCK_KINDS` so the interview and the writer quartet cannot drift apart. **`create_profile`** is the operator's door: bootstrap-scoped until sign-in lands, refusing a friend key adversarially, and writing the new row through the ONE sanctioned re-scope (`session.adopt_owner`) because the app role's own `WITH CHECK` means a profile row can only be written AS the owner it belongs to — proven on the real policy both ways, refused without adoption and created with it, then rolled back. A scan now pins `set_config` to `session.py` alone: anywhere else it would be a tool smuggling identity past the door. The owner's own setters landed with it: **`set_notification_channel`** stores the one secret an owner brings (the ntfy topic IS the capability to reach their phone) and returns `{updated}` — the value is never echoed and the audit row records that a change happened, never what it was — and **`set_notion_token_ref`** stores a pointer, refusing anything token-shaped outright. Both write only the caller's own row: as owner A, A's update touches one row and an aimed shot at B's touches zero, even when the application deliberately passes the wrong owner. **M2/M3** (server instructions + tool annotations) went in as the mechanical warm-up; **M4** typed the six loop tools' output envelopes and **stopped there deliberately, with the boundary itself a test**, so a 46th typed tool is a decision rather than drift. Tools **41 → 45**. One explicit deferral, recorded as a decision: per-owner Notion filing waits for the first real second owner who wants a board — the ref is stored, nothing reads it, and B-GAE-027 already pins that the refusal must be REPLACED rather than deleted when it comes.

- 2026-08-12 01:40 BST — **Phase 9, task 3: the night splits in two — world work once, a personal pass per owner.** The eight world stages (register, classify, discover, fetch, read, synonyms, merge, jd_drip) run ONCE however many owners exist; the seven personal ones (promote, salary, deadlines, eval, stage_reading, file, nudge) run once per owner with their own rule, apply window, tray, board and channel. **The pinned stage order did not move** — the split is derived from `STAGE_CMDS` by a name set rather than by restructuring it, so the 15-stage contract the status page renders is untouched. Written for fan-out, not as a serial loop: `pipeline.owners.task_shard()` reads Cloud Run's `CLOUD_RUN_TASK_INDEX`/`TASK_COUNT` and falls back to 0-of-1, so today's single-task job is every owner serially and raising `taskCount` fans the same code out with no edit (0012 §6.1). The run report folds N owners back into **one array element per stage name**, because `v_status_stages` counts those elements and the public page renders the count — per-owner detail rides as an added `owners` field whose entries are **numbered, not named**, since `pipeline_runs` is world-readable and a profile_id is the exact value a cross-owner read attempt needs. Two defects fell out of writing it, both the same shape (a per-owner value read blind, in code no MCP tool touches — which is why task 1b's sweep could not have caught them): **B-GAE-027**, the nudge stage selecting every owner's eligible rows, sending them to the first profile's phone and then *stamping* them so the real owner was never nudged at all; and **B-GAE-028**, the deadlines stage open-coding `order by created_at limit 1` for `apply_window_days` and applying one owner's window to everyone's listings. Both **measured against unmodified HEAD with a probe rather than asserted**, because the fixes add a required argument and a `TypeError` proves nothing about leaking. Per-owner Notion filing REFUSES with a reason for owners the single credential does not belong to, rather than writing their cards to someone else's board — recorded as a deferral, not a fix. Measured: **876 passed / 37 skipped** offline, **913 passed** with `RUN_DB_TESTS=1`.

- 2026-08-11 21:05 BST — **Phase 9, task 3 (first half): B-GAE-018 closed — the dedupe key is per company, so one tenant can no longer swallow another's adverts.** The aggregator feed is shared, so two owners whose lenses overlap see the same advert; each already had their own `target_companies` row, so both produced the same `dedupe_key` against a different `company_id`. With the unique index on `dedupe_key` ALONE the first owner to merge created the listing and the second was handed **the first owner's `role_id`** — silently never receiving the job. Migration **0058** replaces it with `UNIQUE (company_id, dedupe_key)`; all three callers scoped in the same change (`discover/merge.py` conflict target + fallback lookup, `persist/fetch_rules.py` conflict target, `history/events.py`'s key→role_id map, whose `company_id` is now REQUIRED so no caller keeps the old behaviour by default). The guard needed a real database and no fake could have caught it — a `FakeCursor` has no unique index, which is why all eleven offline merge tests passed throughout the bug's life; `test_two_owners_merging_the_same_advert_each_get_their_own_listing` was seen red for the documented symptom, paired with `test_one_owner_still_cannot_hold_the_same_advert_twice`. Three existing tests pinning the old contract were updated deliberately, not relaxed. Deployed `000c9d6` and **hand-run before cron: run 13, 15/15 stages ok** (fetch 118 jobs exercised the new upsert live; merge had no pending ads, so its insert path is covered by tests only). Four lanes green (tests: **872 passed / 0 skipped** with `RUN_DB_TESTS=1`; 838/34 offline; no-`.env` 838/34; container 798/74).

- 2026-08-11 19:40 BST — **Phase 9, task 2b: the MCP door cut over to `goal_a_app` — RLS now enforces in production.** Since 2a the policies existed on all 28 tables and refused nobody, because every door connected as `postgres` (`rolbypassrls`). New `src/mcp_server/session.py` (75 lines) wraps `get_conn` as `scoped_conn`: resolve the owner FIRST on the engine role, THEN `SET LOCAL ROLE`, THEN publish `app.owner_id` — an ordering that is load-bearing, since `access_keys` and `profiles` both carry owner-keyed policies. Seven one-line import changes; all 26 tool call sites untouched. **The proof had to remove the application filter to mean anything**: because the fakes still patch `<module>.get_conn`, the offline suite passes without ever exercising the role switch — so `tests/test_rls_cutover.py` drives a real tool over a real `Client` with `fetch_queue`'s owner predicate deleted. Seen red twice first (owner B read 20 of owner A's queue rows; the body ran as `postgres`). Scope decided and written into the module: `transport.py`, the nightly job, the dashboard and the status page stay on `postgres` — **anything a key holder can reach runs as `goal_a_app`; everything the founder alone reaches does not.** **B-GAE-023** found and fixed inside the sitting (never shipped): `submit_reading`→`accept_reading` runs `delete from role_skills` and the role holds no DELETE — invisible to 868 green tests, since no fake enforces a privilege. Migration **0057** grants DELETE on that one derived table (founder's call); `test_the_app_role_cannot_delete_anything` rewritten to assert the grant set EQUALS `{role_skills}`, plus a class guard scanning `src/` for every INSERT/UPDATE/DELETE against `has_table_privilege`. All 19 read tools byte-identical under the app role against live data. Deployed `8285af1` (tests: **870 passed / 0 skipped**).

- 2026-08-11 18:10 BST — **Phase 9: three logged bugs closed red-first, and CI green again for the first time since run #28.** **B-GAE-021** — `scripts/mint_access_key.py` entered `get_conn()` (which loads config) before checking `--label`, so its refusal test could only pass where a `.env` exists; the laptop had one, CI and the container do not. That was run #28's 60-second failure. The check moved above `get_conn()`, and the new guard monkeypatches `get_conn` to raise so it fails **on the laptop** — which the old test could not. **B-GAE-020** / **B-GAE-019** — the two stale scaffolds the independent verifier found: `review_items` and the census tables became `(like public.X including all)`, and `insert_census_jobs` gets the `local_matcher` it had been missing since 8.5 task 2. **B-GAE-022** logged new: `census_jobs` also lacked `is_local`, hidden behind 019's `TypeError`, so 019's named one-line fix would not have gone green — found by reading the live schema instead of trusting a tick. Four lanes proven, and the no-`.env` lane re-run with the fix reverted to confirm it reproduces CI's failure rather than passing for its own reasons. Deployed `ffa8561`, hand-run **15/15** (run 12). Tests: **866 passed / 0 skipped** with `RUN_DB_TESTS=1` (868 collected).

- 2026-08-10 18:00 BST — **Phase 8.5, U8b (the founder's 17:15 mid-session amendment, caught at phase close): the cv_blocks writer quartet.** `git log` surfaced parallel commit `f1a5908` adding U8b to plan 0013 ("writer tools ship WITH task 0") — spec without code, so the phase re-opened. Built: `add_cv_block` (ALWAYS a draft — a client proposes, only the owner confirms; kind whitelist mirrors the render sections), `list_cv_blocks` (both states, for approval), `confirm_cv_block` (the owner's yes, audited), `retire_cv_block` (a STAMP — keep-all; and `load_cv_blocks` now excludes retired rows everywhere). Migration **0049** (`retired_at` + `source` provenance). Tools 37 → **41**; CLAUDE.md/relay/README/dev.md re-measured and corrected; snapshot rebuilt. U8b acceptance holds: a user adds a project by conversation, sees the draft, confirms it, and it serves in the next CV payload — zero operator SQL. Red first (8), then green (tests: **774 passed / 10 skipped, exit 0**, 784 collected).

- 2026-08-10 17:53 BST — **PHASE 8.5 COMPLETE — all 9 tasks in one session, and the phase closed with the 6-step relay.** The universal product layer is live: a lens is ROWS set by conversation (words → codes → the rule row → sweep/queue/tray/CV), any industry, no code edits — proven by the care-lens acceptance (integration walk over the real MCP server + the read-only live twin) and by the founder's product staying byte-identical (queue 272→272 across the view change). One session shipped: migrations **0044–0048** (skills learned_at + dedupe guard; the lens row's adzuna_category; the universal queue gate + v_sponsor_browse + fit/new-today; security_invoker restore; the tray's near-miss tier), MCP tools **30 → 37** (find_industry_codes, add_skill, search_sponsors, search_hiring, skip_reading, serve_cv, submit_cv; run_sweep's flag renamed owner_lens; set_promotion_rule knocks on demand), one new 06:30 stage (**jd_drip**, live-smoked, pin updated — ⚠️ first push requires rebuild + manual `gcloud run jobs execute --wait` before cron), and the suite **616 → 767 passed / 10 skipped, exit 0** (777 collected), red-first throughout. Relay executed: decision + progress logs written per task, `CLAUDE.md` archived (`archive-v2-phase8p5-build-instructions.md` + README row), **Phase 9 `CLAUDE.md` staged** (friend keys → RLS-by-refusal → per-owner pass → Google sign-in LAST, per the 13:20 identity decision), relay prompt saved to `docs/handoffs/phase-9-relay-2026-08-10.md`, public snapshot rebuilt and verified (⛔ push waits for the founder's word). Applications: **0** — the number the whole machine exists to move; the queue is open every morning.

- 2026-08-10 17:48 BST — **Phase 8.5, Task 0 (U8): the serve-all CV — ungated mid-session (cv_blocks 0 → 22, the founder's parallel seed) and built to the decided design.** New `src/cv/serve_all.py` + `src/mcp_server/cv_tools.py` (tools 35 → **37**): `serve_cv` hands the client AI the job + **EVERY confirmed fact block** (anti-filter pinned by test — a hint can never become a filter) + `skill_hint` + the versioned server-side `cv-v1` prompt; `submit_cv` traces every bullet against its own block's fact_text (reusing `cv.truth.trace_bullet` — invented numbers die, <75% grounding falls back to the verbatim fact), drops-and-reports unknown blocks, then the ENGINE renders the ATS-safe .docx (`cv.render`) and saves it (`cv.filing.save_cv`); audited; the bytes never ride the wire. The engine-side `generate_cv` stays as the no-AI fallback. Live fact base verified read-only: 22/22 confirmed (8 achievement · 6 role · 6 skill_evidence · 2 education) — never printed. Red first (10), then green (tests: **767 passed / 10 skipped, exit 0**; README → 777/37).

- 2026-08-10 17:41 BST — **Phase 8.5, Task 6: the end-to-end lens ACCEPTANCE — a care-home lens by conversation alone, proven twice.** (1) `tests/test_lens_e2e.py`: five steps over the REAL in-process MCP server — "care homes" → ranked codes (translator); confirmed codes → lens row + auto-knock fires; the SAME `pick_owner_lens_batch` picks care cards from the rule row; the tray stages "Senior Care Assistant" as a match and "Software Engineer" as a labelled near-miss under care patterns; and a source scan pins that no universal-chain module bakes an industry (no SOFTWARE_SIC outside probe_pick's documented bootstrap, no baked codes, no baked category). (2) The read-only LIVE twin (17:39 BST): care doors **49/5,087 knocked (0.96%)** — the auto-knock's first 2,000×4 batch reaches ~40%, run two crosses the U4 >50% target; **313 open care listings already at register sponsors** (ads merged them — day-1 queue is non-empty, with receipts); **0/313 have JDs** ↔ **413 care-titled Reed ads** await the drip (~2 nights at cap 200); the tray then serves them. The founder's live lens was deliberately NOT flipped (decision log 17:40). Tests: **758 passed / 10 skipped, exit 0** (768 collected).

- 2026-08-10 17:38 BST — **Phase 8.5, Task 5b (U7): the tray un-starves — a labelled near-miss tier the client AI may accept or skip.** `stage_ready` now stages two tiers: title matches (uncapped, as ever) and `near_miss` (stageable but title-unmatched — the rows the old sieve dropped entirely, 0 of 1,083 measured; capped 25/run, newest first); kill keywords exclude both. Migration **0048**: `role_listings.staged_tier` + `reading_skipped_at`. `get_reading_batch` serves the tier label with matches claiming first (ordering pinned); new **`skip_reading`** tool (35th, audited) stamps a pass so a skipped row never re-stages (keep-all: removals are stamps); `accept_reading` clears the tier on upgrade. The extraction prompt is deliberately unchanged — relevance judgement lives in the tool contract, not the extraction instructions. Advisors: baseline only (0047's view fix confirmed). Red first (9), then green (tests: **753 passed / 10 skipped, exit 0**; README → 763/48/35).

- 2026-08-10 17:32 BST — **Phase 8.5, Task 5 (U5): the Reed JD drip — ad-only jobs gain full descriptions nightly.** New stage `jd_drip` between merge and promote (DELIBERATE stage-order change, pin test updated same commit) so freshly merged ad rows get full JDs and are enriched the same run. Engine: `src/fetch/jd_drip.py` (`pick_drip_batch` — open Reed-merged JD-less rows, queue-first then newest; `run_drip` — reads the SHARED reed quota ledger first, never exceeds 950/day, ledgers every attempted call, per-item commit + isolation) + `reed_job_details` in aggregators (the one Reed HTTP home; `clean_html` aliases the ONE stripper in fetch/feeds). Pool measured: **3,707** open Reed ad rows without JDs (Adzuna has no details API — waits on boards). **Live smoke test: `--cap 2` fetched 2 real JDs (5.2k clean chars, 0 HTML tags, both in-queue rows), ledgered 2 calls.** ⛔ At push time the 06:30-lane rule applies: rebuild → `gcloud run jobs execute goal-a-daily --wait` → then cron. Red first (8), then green (tests: **748 passed / 10 skipped, exit 0**; README → 758 and 15 stages).

- 2026-08-10 17:25 BST — **Phase 8.5, Task 4 (U6): the dashboard browses the whole sponsor world — and the queue view's hidden founder regex is gone.** Migration **0046**: `v_apply_queue`'s hardcoded title regex (a personal hardcode the 0013 audit missed — it lives in a VIEW, and it made the care-lens acceptance impossible) became a data-driven EXISTS over the owner's `target_roles` (normalised substring; 10 behaviour-preserving patterns seeded; **verified 272 → 272 rows, 0 lost**); `v_today` gained `is_new_today` + `skill_have/skill_asked` (fit receipts); `v_scorecard` gained `new_today` + `sponsors_total`; new curated `v_sponsor_browse` (98,638 registry-matched sponsors, plain-English industries, never the token). Migration **0047**: restore `security_invoker` on the four replaced views (CREATE OR REPLACE drops reloptions — caught by get_advisors as ERROR the minute after, fixed same session). Dashboard: Sponsors tab (zero-JS GET filter form; filtered pager total via `count(*) over()` — no extra counting queries), "skills h/a" + "new today" chips, header new-today count; the complexity pin now REQUIRES `v_sponsor_browse` and still bans every raw table. README numbers table refreshed (751 tests · 47 migrations · 34 tools). Red first (9), then green (tests: **741 passed / 10 skipped, exit 0**).

- 2026-08-10 17:13 BST — **Phase 8.5, Task 3 (U3): the universal read tools — any industry answers by conversation.** Two new MCP tools (32 → 34): `search_sponsors` (plain-English industry / town / board-status over `v_sponsor_industry`; the view's industry_descriptions ARE each row's receipt; boards-first ordering; never the token) and `search_hiring` ("who is hiring \<role words> and can sponsor?" — tracked open listings first, census sightings fill up to the cap). `get_skill_gaps` generalised in place with optional `role_words` (per-lens gap, owner-scoped i_have_it) — no duplicate tool. One shared tokenisation: `criteria.lens.word_patterns` feeds translator + all three searches. Engine logic in `census_queries.search_sponsors` + new `src/analysis/search.py`. Live acceptance ran the exact SQL: **"care-home sponsors in Leeds with live boards" answers over the existing view** — and shows nearly all care cards unknocked, the exact truth U4's knock exists to fix. Red first (14), then green (tests: **735 passed / 10 skipped, exit 0**; README pin → 745).

- 2026-08-10 17:05 BST — **Phase 8.5, Task 2b (U4): knock-on-demand + the honest doors line.** A change to the lens codes via `set_promotion_rule` now starts the owner-lens door-knock DETACHED by itself (batch 2,000 · 4 workers · sweep-lock safe · `knock:{started, log_path}` in the payload, next-hint → `sweep_status`) — a fresh lens no longer waits at 0.7% coverage for someone to remember to sweep. The brief gained `lens_coverage` ({knocked, total, pct} over the exact slice the Pass-2 picker sees) via new `census_queries.lens_coverage`, and `daily_brief` appends *"your industry's doors are still being knocked — N/M done"* while pct < 50 (the founder at 98% never sees it; pinned both ways). Tweaks that don't change codes never knock (pinned). The 06:30 lane untouched — `assemble_brief` is only called by the MCP skin (verified). Red first (8), then green (tests: **722 passed / 10 skipped, exit 0**; README pin → 732).

- 2026-08-10 16:57 BST — **Phase 8.5, Task 2 (U1, the keystone): the Pass-2 probe now picks by the OWNER'S lens — the audit's one real blocker is gone.** `pick_owner_lens_batch` reads the owner's `promotion_rules.industry_codes` (SOFTWARE_SIC survives only as the rule-less bootstrap); a care-home rule and a software rule drive the SAME code path with different rows, pinned by test. Runners renamed `run_lens_sweep[_parallel]`; script flag `--owner-lens` (old `--software-only` kept as alias); MCP `run_sweep` arg is now `owner_lens` (clean rename). The Adzuna ads category joined the lens row — migration **0045** (`promotion_rules.adzuna_category`, founder seeded `it-jobs` so his sweep is byte-identical), `save_rule`/`load_rule`/`set_promotion_rule`/`get_promotion_rule` all carry it, and `scripts/agg_sweep.py` resolves CLI → rule → bootstrap, with `'all'` meaning the whole-inventory walk. `TECH_NAME_PATTERN` untouched (ordering only). The 06:30 lane untouched (daily never imports probe_pick — verified). Red first (21 failing), then green (tests: **713 passed / 10 skipped, exit 0**; README pin 713 → 723 collected).

- 2026-08-10 16:45 BST — **Phase 8.5, Task 1 (U2): words→codes translator + skills entry — a lens is now settable by conversation alone.** Two new MCP tools (30 → 32, pinned set updated deliberately): `find_industry_codes` — deterministic token search over `sic_codes` with prefix-stemming ("care homes" finds the residential-care codes even though no description contains that phrase), ranked tokens-matched-then-sponsor-count, every candidate carrying its receipts (matched words + census sponsor count), pure read whose next-hint hands the confirmed codes to the EXISTING `set_promotion_rule`; and `add_skill` — the FIRST writer of `my_skills`, owner-scoped upsert by `skill_norm` via the one normaliser, `learned_at` + `evidence` pinned from day one for the future learning-curve model, audited. Migration **0044** (`learned_at` column + unique `(owner_id, skill_norm)` guard, measured clean before applying: 22 rows, 0 dupes); engine logic in `src/criteria/lens.py` + `writer.add_skill`, skin in `src/mcp_server/lens_tools.py`. Live-data check: "care homes" → 87300/87900/87200/87100 lead with 1,000+ sponsors each, noise sinks. Red first (18 failing), then green (tests: **703 passed / 10 skipped, exit 0**; README count updated 696 → 713 with the honesty pin).

- 2026-08-10 13:55 BST — **PHASE 8 COMPLETE — THE FLIP SHIPPED, and the phase closed with the 5-step relay.** Task 7 executed on the founder's word. The repo check he asked for first discovered an **unrecorded early flip**: public repo `JOB-ENGINE-`, one commit authored by him 2026-08-03 19:19 (the walkthrough evening), never mentioned in any phase record. Full leak battery on a clone: **clean** (placeholders and fixtures only — it predates the security pass and got lucky), but its README was stale and partly false (592 tests vs 686; advertised the spend cap retired the day it was pushed). **Chose one shop window over two:** fresh snapshot rebuilt from `a2f8fe6` (`SNAPSHOT CLEAN` 4/4 · history depth 1 · LICENSE present), verified beyond the script's own checks by running the **full suite INSIDE the snapshot — 686 passed / 10 skipped, exit 0** — then force-pushed over the stale commit: **`7d8cf62 → adbdf31`**, single commit, live at `github.com/shayandas862-hub/JOB-ENGINE-`. On the way, a **July-20 local export containing `ops/apply-shortlist.md`** (the exact task-6 leak) was found at the snapshot script's default target with its own `.git` — proved remote-less (never pushed; the leak never left the laptop) and deleted with the founder's authorisation. Two parallel-session commits (`1b3994b` serve-all CV, `03270e4` plan 0013) landed mid-flip at 13:07/13:41 and are **deliberately excluded** — publish exactly what was verified; the next refresh carries them. Relay executed: decision + progress logs written, `CLAUDE.md` archived (`archive-v2-phase8-build-instructions.md` + README row), **Phase 8.5 `CLAUDE.md` staged** from its card + plan 0013's U1–U8, relay prompt saved to `docs/handoffs/phase-8-5-relay-2026-08-10.md`. Also this morning, mid-session: macOS revoked the app's Desktop-folder permission (every read "Operation not permitted", home dir + /tmp unaffected — the Files-and-Folders gate, not the project); founder re-granted via System Settings and work resumed. Portfolio milestone met. Applications still 0 — the door this phase built exists to move that number. (tests: 686 passed + 10 skipped, exit 0; record-ids violations: none)
- 2026-08-10 12:20 BST — **Run 8: the first TRUE unattended 06:30 morning — and its silence diagnosed to the row, read-only.** Cloud Scheduler fired **05:30:00 UTC to the second** as `goal-a-invoker`; run 8 **ok, 14/14 stages, 05:30 → 05:43 (12m35s)** on image `c557a23` — digest-verified as the exact artefact run 7 proved by hand, honouring the standing rule. No phone push arrived and the founder asked why: audited rather than assumed. The nudge stage recorded **"nothing new to nudge"** — the only stamp event in the whole table is run 5's **64 listings at 18:58 UTC Saturday** (the cron-move test claimed the morning's queue early, so Saturday evening's push WAS this morning's notification), runs 6/7/8 all found the queue already claimed, and `eligible_unnudged = 0` verified by query. Had the push been attempted and failed, the stage would have failed loudly and withheld the stamps — it passed in 0.7s. **Silence = healthy day with nothing new, by design** (phone = new roles · status page = did it run · email = it broke). Also measured: `promote` said `cap_hit=True, promoted=0` for the 4th straight run with the borderline-review list FULL at 20/20 — the machine is waiting on founder decisions, and the queue holds 169 ready rows against 0 applications; fetch stored 6,490 jobs across 81 boards (8 stale closed) with Starling Bank's 429 tolerated per-company; the tray staged 0 of 1,083 again (plan 0013 U7). No code touched; the day's other product: the full go-live + OAuth/dashboard/multi-user plan delivered to the founder (Google sign-in decision → decision log 13:20). (no code changes; suite re-measured 686 passed + 10 skipped, exit 0)

- 2026-08-09 23:55 BST — **CI stops rebuilding the image for prose (founder-initiated, commit c557a23).** The founder asked whether repeated cloud runs cost money and said his instinct was to fix this properly rather than "rebuild the whole car because the owner's manual changed". Measured rather than guessed: Cloud Run's daily job uses **~23,700 CPU-seconds/month against a 180,000 free tier (~13%)**, both services scale to zero, and Artifact Registry holds **380MB across 20 images** — under the 0.5GB free tier. So **Google is effectively free**; the real meter is **GitHub Actions on a private repo** (2,000 free minutes/month, 8–12 per full run), where ~16 pushes in one evening spent **~8% of the month**, many of them for log entries. Fixed with a `changes` job that asks whether anything shipped by the Dockerfile's COPY allowlist actually moved; on a doc-only push the `image` job skips and `deploy` skips with it, so the deployed artefact stays put. **The `test` job is deliberately NOT gated** — the suite reads the README and the git index on purpose, so documents genuinely can turn it red; skipping tests would have been the cheap, wrong version. An unknown base sha assumes the program changed, because a silently-skipped deploy is exactly the class of quiet failure this phase kept finding. The correctness argument outranks the cost one: rebuilding on every push made the "hand-run the 06:30 image first" rule impossible to follow, and it silently was not followed tonight — `0836977` was proven by hand, then two markdown edits swapped in `682def3` beneath it (harmless, pipeline diff measured at zero files, but a rule that quietly cannot hold is worse than none). **This very log entry is the live test of the fix: it is a doc-only push, so the image must NOT move from c557a23.** (tests: 696 collected, 686 passed + 10 skipped, exit 0)
- 2026-08-09 23:30 BST — **The 06:30 lane re-proven on the image cron will actually meet, and C3 finally proven LIVE.** Tonight's CI auto-deploys moved the daily job past `b8dbab9` (the last image executed by hand) to `0836977`, so the standing rule — rebuild → manual execute → only then let cron meet it — was owed a run. First measured the blast radius rather than assuming: `git diff b8dbab9..HEAD -- scripts/ src/` excluding `src/status` and `src/mcp_server` is **EMPTY**, i.e. the nightly code path is byte-identical to the one that went 14/14 by hand; everything that changed is docs, tests, CI, ops, and the NEW `src/status/` package that `run.py` never imports. Executed anyway, because the rule is about the ARTEFACT, not the source: **run 7 `goal-a-daily-44tzb`, status ok, 14/14 stages, 23:13:35 → 23:27 UTC**. **The nudge stage said "nothing new to nudge" for the THIRD run running** (runs 5's 64 stamps still stand), so the push path was still not exercised by the pipeline — which is exactly the gap that would have been quietly carried into tomorrow morning. Closed it deliberately through the HOSTED MCP on the current image: `send_test_nudge` → `channel_configured: True, sent: True`. **That call is the live proof of C3 specifically:** its title is `"GOAL A — test nudge"` and the em-dash is precisely the non-latin-1 character that used to raise `UnicodeEncodeError` — which is not a `requests.RequestException`, so it escaped the handler and made this tool crash every single time. It now delivers from inside Cloud Run. One notification arrived on the founder's phone as a result, with his prior agreement. So both halves of "is tomorrow safe" are answered by measurement: the artefact runs green, and the phone path works on that same artefact. (tests: 685 passed + 10 skipped, exit 0)
- 2026-08-09 23:18 BST — **PHASE 8 TASKS 6, 5b AND 7-PREP DONE — only the act of publishing remains.** **Task 6 — pre-flip security pass.** The `security-review` skill was NOT run and the substitution is recorded: it reviews the diff against `origin/HEAD`, that diff is **0 files**, so it would have reported clean after reviewing nothing. Did the whole-repo audit the card asks for instead, and found **two real leaks in tracked files**: `ops/apply-shortlist.md` (15 named roles the founder is targeting at Anthropic/Faculty/Palantir with live application links — untracked, local copy kept, `.gitignore` widened) and the live Supabase project ref in `PROJECT-MEMORY.md` (scrubbed). **Cleared by measurement:** the two `AIza` strings are **6 and 10 characters** (truncated placeholders, not the 39-char real thing), `.env.example` holds bracketed templates, `docs/cv-intake-template.md` is an **empty form**, and no email, phone or founder company appears anywhere. New `tests/test_public_safety.py` (7 tests) asserts all of it **against the git index** — what would actually be published — with each detector first proving it can BITE on a synthetic bad value, because the two written naively both cried wolf immediately. Module-level `skipif` when `.git` is absent, verified to SKIP (not error) in a replica of the image layout — the `83aa52c` trap avoided deliberately. **Task 5b — deploy on green, with no service-account key in existence.** `ops/cloud/setup-wif.sh` creates the pool, provider and deployer identity; CI authenticates by short-lived OIDC. **Verified live after creation:** the provider's attribute condition is `assertion.repository=='shayandas862-hub/GOAL-A'`, the only principal that may impersonate the deployer is that repo's principalSet, and the deployer holds **`run.developer` + `artifactregistry.writer` and nothing else** — no owner, no editor, and pointedly no `secretAccessor`. The job is updated but **never executed** by CI (the 06:30 lane stays a deliberate act); the two services roll automatically and the workflow then curls the public page and fails the build unless it answers 200. **Proven live by the push itself:** all three surfaces moved to image `ba69413` automatically, and after the roll the status page still answered **200 anonymous** and the MCP still gave **401 without a token / 200 with it**. Found and fixed live: a just-created service account is not immediately usable as an IAM member, which killed the first run under `set -e` — the script now retries propagation. **Task 7 — prep only.** README rewritten from measured state (it advertised 402 tests, 34 migrations, 24 tools, "caged AI with Gemini" and "a hard spend cap" — reality **694 / 43 / 30 / no AI / cap retired**), now states what the project has **not** done (it has not got the founder a job) and links the live status page; two tests pin it, one of which failed on its first run because writing them moved the count 692 → 694. `ops/flip/prepare-snapshot.sh` builds the public snapshot **locally** — `git archive`, drop `CLAUDE.md`/`PROJECT-MEMORY.md`/`docs/handoffs`, one commit, no ancestry — then verifies the ARTEFACT: **4/4 checks ok, history depth 1, LICENSE present, SNAPSHOT CLEAN**. It publishes nothing. **Three instruments caught lying, all the same shape:** polling the Actions API on a PRIVATE repo returned `{"status":"404"}` and the poller read the error code as the run status (replaced by asking GCP which image the services run); the README test SKIPPED because `addopts="-q"` plus a hand-passed `-q` makes `-qq` and silences the count line; and the snapshot verifier died mid-check because `grep` exits 1 on no-match, `pipefail` propagates it and `set -e` killed the script — a clean snapshot aborting the check meant to bless it. In every case a standalone trial had "passed" under conditions that differed from the real ones. **Remaining in Phase 8: only the flip itself** — creating the public repo, which is the one irreversible act and needs the founder's explicit word. Applications still 0. (tests: **684 passed + 10 skipped, exit 0**; record-ids violations: none)
- 2026-08-09 22:39 BST — **PHASE 8 TASKS 3 AND 4 LIVE — the first Cloud Run SERVICES this project has ever deployed, both founder-gated.** **Task 3 — hosted MCP** (`goal-a-mcp`, image `b8dbab9`): its code half turned out to have shipped already in `05ace84` this afternoon (token gate, rate limiter, `MCP_TRANSPORT=http`, 8 tests) while `CLAUDE.md` and plan 0012 both still listed task 3 as untouched — they were only ever tracking the deployment half, found by reading `server.py` rather than the plan. This session added the deployment half: `MCP_TOKEN` generated by the founder into `.env` (never seen, never printed, `.env` → stdin → Secret Manager, 7 secrets now), the allowlist SPLIT into `SECRETS`/`JOB_SECRETS`/`MCP_SECRETS`/`STATUS_SECRETS` so each surface mounts only what it uses, `test_the_secret_allowlist_is_exactly_the_six` deliberately RENAMED to `..._the_seven` rather than loosened, and a new test that the daily job never receives the MCP token. **Proven against the live cloud URL:** no token **401** · wrong token **401** · right token **200** · **30 tools** reachable (retired `run_pipeline` correctly absent) · `daily_brief` reached the DATABASE from Cloud Run returning *"0 applications so far, 150 to read, 1063 to review"* · the full contract-v2 loop `daily_brief → get_reading_batch` (2 JDs served) `→ submit_reading` · **rate limit provably trips: exactly 15 of 40 concurrent requests passed (the burst capacity), 25 refused** — and it reports `client: global`, i.e. one shared bucket rather than per-token, correct for one user and carried to Phase 9. **Task 4 — public status page** (`goal-a-status`, image `318df2b`): migration **0043** (`v_status`, `v_status_stages`, mirrored to `db/migrations/`, `get_advisors` clean of new findings) plus `src/status/` — the container had routed a `status` door at a module that did not exist since task 1, so that door crashed on every invocation and now serves. **On a surface with no auth the VIEW is the privacy boundary, not the page:** the package does not know the NAME of a single personal column and a test greps its source for eighteen of them; a second test smuggles salary/owner/ntfy fields into the row and asserts none render; stage SUMMARY text is dropped by the view because "nudged 64 listing(s)" names companies and describes the owner's activity; `applications_total` is deliberately absent although `v_scorecard` carries it. **Live, anonymous, verified:** `https://goal-a-status-730809616632.europe-west2.run.app` returns **200 with no credentials of any kind**, 4,467 bytes, unknown paths 404, and a leak scan of what the world actually receives shows **0 hits** for the founder's name, the ntfy topic, the Supabase ref, "£", "applied", "Bearer", "postgres", "password" or an email — the only "salary" is the pipeline STAGE called salary. It serves the real numbers: last run **ok, 14/14**, **144,041** sponsor licences · **128,222** organisations · **314** boards · **104,761** adverts · **12,495** listings · **885** companies · register refreshed **today**. Two instruments caught lying by the standing rule: the first MCP smoke returned 307 for all three credentials (the path is `/mcp`, not `/mcp/` — a uniform redirect is indistinguishable from a uniform result), and a secret-grant loop retyped into this **zsh** shell did not word-split, trying to grant one secret named "DATABASE_URL MCP_TOKEN" while the following `echo` printed "granted" regardless. Also recorded rather than hidden: `/healthz` serves 200 from the same image locally but 404 at Cloud Run's edge, and the hosted MCP **cannot** reach Claude Desktop/web/Cowork because those connectors authenticate by OAuth while our door takes a static bearer (measured: bare `WWW-Authenticate: Bearer`, 404 on all three OAuth discovery paths) — Claude Code works, and OAuth is the founder-chosen next piece because it also unlocks a hosted user dashboard with a real login. Applications still 0. (tests: **671 passed + 10 skipped, exit 0** in the repo — 649 → 671, +22; **652 passed + 29 skipped, exit 0 inside the real image**; record-ids violations: none)
- 2026-08-09 21:20 BST — **PHASE 8 STAGE C COMPLETE — all six fixes shipped, deployed and proven in the artefact (commit b8dbab9, image `b8dbab9` live on the job).** Task 3 is unblocked: the four MCP tools that were dead inside a container now resolve a real interpreter. **C1** `python_executable()` replaces the hardcoded `.venv/bin/python` in `pipeline/trigger.py` + `mcp_server/census_tools.py`, mirroring the `_python()` helper `scripts/run.py` already carried for this exact reason; proven INSIDE the built image — `/app/.venv/bin/python` `exists=False` (the old path, i.e. `FileNotFoundError` on every spawn) while the resolved `/usr/local/bin/python` exists and is executable. **C2** `run_pipeline` split into `preview_pipeline` (synchronous dry run, seconds) + `start_pipeline` (detached, logs to `ops/run-logs/`), killing the guaranteed 504 behind Cloud Run's 300s default; **tools 29 → 30**, all eight `call=`/`Next:` hints repointed. **C3** `headers={"Title": title.encode("utf-8")}`. **C4** `notify_failure` shouts `!!! PIPELINE ALERT UNDELIVERED` on stderr (→ Cloud Logging) on no-channel, refusal, or a fault in the alert path itself, and never raises (it runs before `finish_run`). **C6** launchd plist retired with the founder watching — verified absent from `launchctl` and `~/Library/LaunchAgents` first; it had never been loaded. **The night's real finding was three tests that could not fail:** the `.venv` assertions were true *because of* the bug they were meant to catch; the first C3 regression test **passed against the unfixed code** (`responses` mocks the adapter, so the latin-1 encoding that actually raises never happens) and was replaced by a session double reproducing `http.client.putheader`; the C4 no-channel test guarded with a throwing lambda the new `except` would have swallowed, rewritten as a call recorder. Each made red-for-the-right-reason before its fix. Also added `test_every_next_hint_names_a_tool_that_actually_exists` — C2 moved eight cross-references at once and nothing checked that a `next.call` names a real tool; it tells fields from tools by whether the docstring documents the token inside `{...}` (no allowlist to rot) and asserts its own scan found something, so it cannot pass by matching nothing. Two honest limits recorded rather than papered over: (1) `start_pipeline`'s detach fixes the 504 but a subprocess inside a Cloud Run **service** dies to CPU throttling and scale-to-zero — hosted, it must invoke the `goal-a-daily` **Job** via the Jobs API; written into the docstring so **task 3 inherits the finding, not the trap**; (2) this run's `nudge` stage read "nothing new to nudge" because run 5 had already claimed those 64 an hour earlier, so **no push was sent and the ntfy egress path was NOT re-exercised by run 6** — C3/C4 rest on unit proof plus the earlier live verification, not on tonight's execution. **C5 deployment, measured:** amd64 cross-build pushed as `b8dbab9`+`latest`; job image updated (timeout 5400 and SA `goal-a-runner` preserved); manual execution `goal-a-daily-cmgk6` **succeeded**, `pipeline_runs` run 6 **status ok, 14/14 stages, 20:02:41 → 20:15:43 UTC (13m02s)**; the execution's resolved digest `sha256:d1d04e8b…` confirmed to be the `linux/amd64` child of the `b8dbab9` manifest list, i.e. tomorrow's 06:30 meets an image already exercised by hand — the standing rule honoured. `docs/dev.md`'s schedule section rewritten from measured state (it documented launchd at 08:00 pointing at a now-deleted plist and warned of "daily Gemini spend" retired in August; live cron re-checked against Cloud Scheduler: `30 6 * * *` Europe/London, ENABLED) and its stale "24 tools" corrected to 30. Applications still 0. (tests: 649 passed + 10 skipped, exit 0 in the repo — 637 → 649, +12; 636 passed + 23 skipped, exit 0 **inside the real container image**; record-ids violations: none)
- 2026-08-09 20:30 BST — **Road to fully built written down (plan 0012).** One ordered route Phase 8 → 8.5 → 9, created at the founder's request the same night the machine first ran itself. Carries: measured state (647 tests, migrations→0042, 29 tools with 4 container-broken, 0 Cloud Run services, tray 150 staged / 0 ever read, `cv_blocks` 0, applications 0); six working principles including the new standing rule that *a check must fail loudly and differently from "all clear"*; Stage C placed ahead of the card order because it BLOCKS task 3 (4 dead tools) and its own gate expired when tonight's run passed; a note that tasks 3+4 share the first Cloud Run **service** this project has ever deployed and shouldn't be split across sittings; a warning that `MCP_TOKEN` breaks `test_the_secret_allowlist_is_exactly_the_six` **by design** so a future session doesn't "fix" it by loosening the assertion; an 8-item security-debt register with honest when-it-bites severities (project ref in history → blocks any full-history public push; RLS on with 0 policies while the engine bypasses as owner role); scale analysis concluding the architecture's shape is RIGHT and should not change (shared expensive data, tiny per-user overlay — one census serves everyone) while naming what does break at N users (nightly job is O(1) shared / O(users) per-owner → fan-out via Cloud Run taskCount, not a serial loop; shared per-account API quotas are a hard ceiling; the MCP rate limiter is in-process and wrong the moment Cloud Run scales out); and 7 measurable done-criteria replacing "perfect" — of which only **applications sent > 0** matters to Goal A. Two blockers named that engineering cannot clear: `cv_blocks` (founder's facts session, running in parallel) and the tray starving after its 150 drain. Estimate ≈10–14 sittings against ~14 weeks. No code touched. (tests: 637 passed + 10 skipped, exit 0)
- 2026-08-09 19:20 BST — **Audit remediation Stages A+B shipped; the machine can no longer fail silently.** An independent read-only audit found three critical risks before the first unattended 06:30 run; triage was re-staged after tracing the call graph (only risk 1 is on the 06:30 path; risk 3 needs a rebuild and was deliberately held until after tomorrow's run). **Stage A — the silence closed:** 32-hex ntfy topic generated and subscribed (laptop + phone), written to `profiles.notification_channel` (was NULL — the cause of "64 ready but no notification channel configured" in cloud run 4), and proven through the REAL code path, not a curl: `send_test` and `notify_failure(['DRILL-not-a-real-failure'])` both returned True and landed, plus a full `digest()` carrying `Société Générale —— Ünicode Ltd` to prove UTF-8 bodies survive. Plus the independent second channel as code (`ops/cloud/alert-job-{failed,missing}.json`, commit c0d6894): two Cloud Monitoring policies on an email channel to the project owner, watching the infrastructure that `notify_failure` structurally cannot report on. **Stage B — git hygiene + CI:** commit 8259b69 pushed (the image running in Cloud Run had existed on one laptop only); `.gitignore` gained `ops/*.log` + `ops/run-logs/`; `.github/workflows/ci.yml` added (offline suite on push/PR, then build the container and run the suite INSIDE it — deploy-on-green deliberately omitted pending Workload Identity Federation, no SA key exported). **CI earned its keep on run one:** it found the container suite had been red since 7fd4977 (`tests/test_cloud_setup.py` reads `ops/cloud/*.sh`, which the image excludes by design) — fixed with a module-level skipif matching the `test_record_ids.py` precedent, verified against a REAL locally-built image (build exit 0, 624 passed / 23 skipped / 0 failed, unknown door exits 2). **Second bug found by running code rather than reading it:** `send_push` crashes on any non-latin-1 title because `UnicodeEncodeError` is not a `requests.RequestException` and escapes the handler — `send_test` therefore always failed. The 06:30 path is unaffected (digest titles are ASCII, `notify_failure`'s is a literal) and the fix is verified; queued for Stage C. **THE MACHINE NUDGED THE FOUNDER BY ITSELF — first time ever.** Rather than force a run or wait for 06:30, the founder directed the stronger test: move the live cron to `45 19 * * *` and watch the scheduler wake the job on its own (a forced `jobs run` bypasses the very trigger under doubt). It fired at **18:45:00 UTC to the second**, `RUN BY goal-a-invoker@…gserviceaccount.com` — a robot, where both prior executions name the founder. Run 5: **status ok, 14/14 stages, 18:45:16 → 18:58:16 UTC (13m00s)**, `nudge` stage "nudged 64 listing(s)". Because `nudged_at` is stamped only after a successful send, those 64 stamps are themselves proof the CONTAINER reached ntfy.sh — the last untested link, closed. Confirmed three ways: DB stage summary, the message on the ntfy server (title "64 roles ready to apply" — Anthropic Applied AI Security Architect + 4 Palantir roles), and the founder's phone. Live cron restored to `30 6 * * *` immediately (env.sh never edited). One more lesson banked: the monitor watching for the scheduler reported "Scheduler did NOT invoke the Job" while it was running — zsh does not word-split unquoted `$P`, gcloud errored, and `2>/dev/null` hid it; caught only by checking live state instead of trusting the instrument. Same failure mode as the night's other three findings: **a broken check looked exactly like a clean result.** Production image untouched at 8259b69; four commits pushed (8259b69, ad36e15, c0d6894, 83aa52c). Applications still 0 — but for the first time the queue reached the founder unasked. (tests: 637 passed + 10 skipped in the repo, exit 0; 624 passed + 23 skipped inside the container, exit 0)
- 2026-08-03 — **Daily classify top-up shipped + walkthrough findings filed.** Founder's step-by-step product walkthrough found the gap: the weekly register refresh cards newcomers but classification only ran by hand. Fixed same day (commit 5a59c4e): new `classify` stage between register and discover — never-asked cards only, `--batch 2000` cap, quiet exit-0 on no-key/census-current, stage-order pin updated deliberately; built by a delegated Opus agent in a worktree from a written spec, reviewed line-by-line + full suite before merge. Dashboard v2 (tabbed lanes + pagination, ThreadingHTTPServer) also landed this session (commit 06671ae). Every other suggestion from the walkthrough written to `plans/0010-walkthrough-findings.md` (13 items with why/how/trigger: registry-error retry, universal search_sponsors + role/skills lens tools, Sponsors browse tab, Reed JD drip, reader-supplies-JD trust design, more ATS adapters, Workday intake). Applications still 0. (tests: 592 passed + 10 skipped, exit 0)

- 2026-08-02 20:10 BST — **PHASE 7.8 COMPLETE — all nine tasks in one session, suite 441 → 581 (exit 0), migrations 0037–0041, every pre-existing test green.** The two halves are ONE MACHINE, verified live: (1) math core `src/match/` — overlap×rarity scoring, percentiles, Laplace confidence, half-life decay, every number recomputable from its receipts; (2) `prob.py` name ladder (exact > suffix-stripped > Monzo-class brand prefix > overlap) + same-job evidence weighing; (3) **the merge ran live: all 9,196 matched ads drained — 5,549 became role_listings rows · 840 absorbed as duplicates by prob.py · 2,805 recruiter-matched (census SIC-78) kept out · 763 boardless companies entered audited** — `v_today` now shows adzuna 63 · reed 37 beside ashby 43 · greenhouse 16 · lever 8, one queue at 200 rows (96 ready / 104 needs) with the fit regex keeping the morning lane calm; (4) **the promote button became a rule** (promotion_rules 0038, founder's rule seeded from SOFTWARE_SIC): first nightly-style pass promoted 25 census boards, flagged 20 borderlines with the cap holding exactly, 103 silent — tracked companies 79 → 867, boards 39 → 64; (5) **sieve-3 reading tray** (0039): stage/serve/accept with the versioned server-side prompt extract-v1 and the deterministic grounding gate (read.eval — one truth-gate, three callers); 70 JDs staged, keyword fallback untouched as the engine default; (6) **MCP contract v2: 29 tools**, every result {result, next(state/call/why)}, every description What/When/Returns/Next, daily_brief/get_reading_batch/submit_reading/get_promotion_rule/set_promotion_rule live, skin still logic-free (pins updated consciously); (7) **survival deadlines** from listing_events (first-appeared→first-closed, MIN_SAMPLE 5, receipts carried), wired into enrich_deadlines with the flat window honestly labelled; (8) **the Today dashboard** (0040 views v_today/v_scorecard/v_health): token-gated 127.0.0.1 page, ready-vs-needs with receipts per row, honesty panel, company watch — complexity-hiding pinned (reads ONLY the views; verified live 200/403 rendering real data); (9) **heartbeat + register refresh**: run.py = register → discover → fetch → read → synonyms → merge → promote → salary → deadlines → eval → stage_reading → file → nudge (order pinned); **first automated register refresh: +1,853 licences added (1,664 new orgs carded) · 1,242 stamped removed · register 143,809 rows, age 0 days** — the 45-day blind spot closed, weekly self-scheduling via `--if-stale 7`. Standing state: **applications still 0** (the queue now holds 96 ready rows with receipts); launchd/ntfy remain the founder's switches, untouched; nothing pushed. (tests: 581 passed + 10 skipped, exit 0)
- 2026-07-31 08:32 BST — HIRING-FIRST PROBE COMPLETE (ran to its own end before the founder's stop; nothing left running). **146/146 sponsors probed → 11 new boards · 196 census jobs · 0 errors**; census boards **279 → 290**, census jobs 6,001 → 6,197. All A-rated, all direct employers by construction: 9fin (ashby, 29 UK jobs, London) · Liberis (greenhouse, 27, London) · Allica Bank (ashby, 17, London) · Wireless Logic (greenhouse, 14, Reading) · Genomics (ashby, 8, Oxford) · Right Formula (lever, 6) · Quadrature Capital (greenhouse, 4) · Xyme (ashby, 1) · plus Sanctuary/Technimove/Four Seasons (boards live, 0 UK today). Same session: the aggregator was relaunched on the founder's word and **self-exited in seconds with ZERO API calls** — all 471 slices already complete, i.e. the collection phase is genuinely converged, not stalled. Standing gap unchanged: **0 applications; last pipeline fetch 2026-07-10**; 9,196 matched ads still unmerged; 290 boards still unpromoted.
- 2026-07-30 08:31 BST — **AGGREGATOR PASS COMPLETE — the wrapper self-stopped** ("every slice complete — the pass is done"), exactly as designed and with no operator action: **471/471 slices complete** across both sources. Final: **104,761 ads · 95,781 distinct jobs · 9,196 at 826 licensed sponsors** (Reed 75,438 · Adzuna 29,323); +2,895 banked today, which is now just the market's daily churn — the collection phase has genuinely converged, not stalled. Run had been relaunched 2026-07-30 00:26 BST on the founder's word ("start the run, keep the flow going") and finished its remaining Adzuna town slices on 107 calls. **Nothing left to collect** without a fresh-pass reset or a wider partition (more towns / contract types). Free work still standing: **146 licensed sponsors with 254 live ads have never been probed for a board** (`scripts/sweep.py --hiring`). Downstream, untouched: promotion review of the 279 boards, the `aggregator_ads → role_listings` merge (9,196 matched ads still invisible to `v_apply_queue`), and — the number that matters — **still 0 applications, last pipeline fetch 2026-07-10**.
- 2026-07-29 08:32 BST — RUN STOPPED by the founder ("stop the run"); cursors/quota intact for exact resume. **The attribute-partition fix is vindicated by one night's data:** ads 61,501 → **101,866** (+40,365; **+29,800 in the 07-29 quota day alone** vs the ~4,000/day the salary bands had decayed to), matched 5,969 → **8,967** at **801 sponsors** (was 683), distinct jobs **93,102**, Reed alone 40,533 → **75,438** (the 44% ceiling is gone). **All 80 Reed town × poster slices completed — no walls hit, nothing stuck.** The poster split is now a stored fact: direct employers 40 slices / 33,749 ads walked (44,648 provider total) vs recruiters 40 slices / 83,611 walked (211,285) — **~4 in 5 Reed ads are agencies**, filterable in one clause. 1,162 matched ads at £50k+. Adzuna: 2 town slices left; both sources were at cap for the day (Reed 929/950, Adzuna 240/240) and the wrapper was napping correctly when stopped. **New free work surfaced:** 134 licensed sponsors with 230 live ads have never been probed for a board (`scripts/sweep.py --hiring`). Resume: `rm -f .aggregator-stop && nohup ops/run-aggregator.sh >/dev/null 2>&1 & disown`.
- 2026-07-28 23:41 BST — SALARY BANDING RETIRED · ATTRIBUTE PARTITIONING SHIPPED, run restarted (founder: "do the fixes, and then start the run"). **Root cause proved by measurement before any code changed:** Reed's `minimumSalary`/`maximumSalary` are OVERLAP filters, not partition filters — a band **£25,155-to-£25,155, zero pounds wide, reports 12,176 results** (a job advertised '£20k-£30k' answers every band between). So every band returned the same ~12k superset behind the same 10k wall; most closed on the saturation guard after exactly 3 pages (`ads_seen=300`), i.e. **324 bands × ~3 calls ≈ a full quota-day spent on bookkeeping**, ceiling stuck at 44% of Reed / 45% of Adzuna. Fix, RED-first: `page_reed` gains `distance_from_location` + `posted_by_direct_employer`/`posted_by_recruiter`; new **`plan_location_slices`** partitions by the OWNER'S OWN register towns × poster type (exclusive facts — each pair opens its own 10k window; case-folded because the register holds 'London' AND 'LONDON' = 35,936 rows that would otherwise be walked twice); new **`retire_slices`** closes proven-futile partitions without walking them (65 leftover salary sub-bands retired = ~195 calls saved); Adzuna switched to town slices too (its category already partitions the other axis). **Suite 434 → 441, exit 0.** Live smoke proved the difference: Birmingham·direct **1,081** / Birmingham·recruiter 4,956 / Manchester·direct 1,166 / Manchester·recruiter 6,046 — real, distinct, sub-wall totals vs. the uniform ~12,000 of salary bands; **yield ~35 new ads/call vs ~11 before**. Run relaunched 23:40 (PID 5947, PPID 1) and immediately walking **London·direct = 4,037 total — under the wall, i.e. reachable for the first time**. State: 61,501 ads · 5,969 at 683 sponsors · 279 boards. (tests: 441 passed + 10 skipped, exit 0)
- 2026-07-28 08:09 BST — DEPTH-WALL RETRY LOOP FOUND AND FIXED (caught by the founder's "status?"): band `reed|smin=50001|smax=100000` held **29,495 ads behind Reed's hard 10k wall** — it was one of `plan_bands`' deliberately-oversized flush leaves (256-band cap) — so page 101 500s forever; `run_slice` called that `source_error`, the wrapper slept 60s and retried the same dead page **638 times**, burning ~317 of the day's 950 Reed calls and blocking every other Reed band. Fix, RED-first: (1) **wall guard** — a failure past `wall_at=9,500` seen rows is the provider's depth wall, not an outage: the slice closes and returns new outcome `depth_wall` (an early failure is still `source_error`); (2) `overall_verdict` treats `depth_wall` as progress (no nap, no error-retry); (3) new **`agg_partition.split_band`** re-splits ONLY the walled range, contiguous by construction, persisting one cursor per sub-band; (4) runner wires it: a walled Reed band re-splits itself in the same cycle. **Suite 430 → 434, exit 0.** Verified live on restart: the stuck band was recognised, closed and **re-split into 65 sub-bands** (Reed pending 1 → 65, ~19.5k previously-unreachable ads now reachable); run relaunched 08:07 (PID 95803, PPID 1) and correctly napped once Reed's remaining quota was spent (383/950 today). State: 60,875 ads · 677 sponsors · 279 boards. (tests: 434 passed + 10 skipped, exit 0)
- 2026-07-27 23:57 BST — AGGREGATOR RUN RESUMED on the founder's word ("start the run"): `.aggregator-stop` removed, wrapper relaunched detached (PID 86135, PPID 1 — Claude-independent), log `agg-20260727T225707Z.log`. Resumed exactly where the 08:33 stop left it (nothing re-fetched); Reed bands walking immediately. Started 3 minutes before midnight, so it runs on the ~500 Reed calls left of the 07-27 ledger, then rolls onto a fresh 950 + 240 at 00:00 for the 07-28 day.
- 2026-07-27 08:33 BST — RUN STOPPED by the founder ("stop the run, I will resume later"): `.aggregator-stop` placed + pkill; all processes confirmed gone; cursors/quota intact for exact resume. State at stop: **56,610 ads · 5,589 at 613 sponsors · 279 boards**; Reed **1 band pending** (450/950 calls used today), Adzuna 20 bands pending (240/240 spent). Overnight results of the 01:13 fixes: **hiring-first probe completed 394/394 → 19 new boards · 857 census jobs · 0 errors** (boards 260 → 279; notable finds: Private Equity Insights 454 UK jobs, Infinitesima 29, Zen Educate 28, Scott Logic 26, Awin 17, Exponential-e 17 — all A-rated, none reachable before); nap fix verified in the wild (drip stayed awake working Reed instead of sleeping out its unused quota, +11,733 ads today). Resume: `nohup ops/run-aggregator.sh >/dev/null 2>&1 & disown` after deleting `.aggregator-stop`.
- 2026-07-27 01:13 BST — THREE LIVE DEFECTS DIAGNOSED AND FIXED, RED-first, run stopped for the hot-file window and relaunched (founder: "fix the real defects… find it first and then fix it"). **Suite 425 → 430 passed, exit 0.** (1) **Token harvest was structurally dead** — diagnostic followed 8 real ad links: Adzuna's `/jobs/details/{id}` AND `/jobs/land/ad/{id}` resolve to *themselves* (no server redirect), Reed's `/jobs/{slug}/{id}` stays on reed.co.uk, so `parse_ats_url` could never match — 3,458 links followed, 0 hints planted, and my earlier "~22 addresses learned" claim was wrong (those 22 are `already_tracked` cards). Root finding that replaced it: **433 licensed sponsors with 5,029 live ads have NEVER been probed** (Pass 2 covered only the software-SIC lot). Fix: retired `src/discover/token_harvest.py` + its tests + the runner's `--harvest` HTTP; added **`pick_hiring_batch`** (unprobed sponsors with live ads, busiest first) + **injectable `picker=`** on both `run_software_sweep`/`_parallel` + **`scripts/sweep.py --hiring`** — ads now say WHO is hiring and the free, tested board probe finds their boards. (2) **Cross-source nap defect** — any capped source made the wrapper sleep 30 min while the other still held quota (Adzuna capped in 29 min, Reed idle with 850 calls): new pure `overall_verdict()` naps only when NOTHING can progress; `page_budget_done` on any source outranks another's cap or transient error. (3) **Status read the wrong quota day** — display used Postgres `current_date` (UTC) while the runner ledgers on the local date, so 00:00–01:00 BST showed the previous day's calls; `_print_status(cur, today)` is now parameterised (verified live: reed 140 today, not yesterday's 950). Live checks: picker returns **434** candidates; first 40 probed → **0 boards** (ordering by ad-count surfaces recruitment agencies, which use recruiter CRMs not startup ATSs — logged as a refinement, full 434 launched since the probe is free). Relaunched 01:12: aggregator drip PID 73947 + hiring probe PID 73958, both PPID 1. (tests: 430 passed + 10 skipped, exit 0)
- 2026-07-26 00:05 BST — AGGREGATOR RUN RELAUNCHED FOR TONIGHT on the founder's word ("start the run for tonight"). State at launch: the 2026-07-25 wrapper was still alive (PID 24679, PPID 1, 21h43m, napping on spent quota — Reed 950/950 + Adzuna 240/240 for the 07-25 ledger day, third consecutive full-quota day); stopped it cleanly and relaunched at 00:04 so the fresh local-day quota starts immediately instead of waiting out its ≤30-min nap. New wrapper PID 99480 (PPID 1), log `agg-20260725T230457Z.log`. Verified flowing within seconds: Reed bands £24,977–24,988 and £24,989–24,994 both walked to `pass_complete`. Baseline at launch: **31,047 distinct ads · 4,069 at 392 matched sponsors**; pending slices Reed 236 / Adzuna 25. Noted (cosmetic only, no functional effect): `--status` reads the quota ledger with Postgres `current_date` (UTC) while the runner keys the ledger on the local date, so between midnight and 01:00 BST the status line shows the previous day's calls — the run itself correctly sees a fresh day.
- 2026-07-25 02:25 BST — ADZUNA SILENT-CLAMP FIX SHIPPED + RUN RELAUNCHED (founder: "you can fix what is needed"). Discovery (caught by the founder's progress-percentage question, 00:15): Adzuna silently stops yielding new ads at ~5k accessible results per query — no error, pages just repeat — so "72% walked" was fake; true distinct coverage was 5,253/44,439 (≈12%). Fix, RED-first: (1) `ensure_reed_bands` generalised to **`ensure_bands(source, …, base_params)`** — Adzuna banded like Reed (26 bands, target 4,500, category rides in every band's params; legacy clamped cursor auto-retired); `page_adzuna` gains salary_min/salary_max; (2) **saturation guard** in `run_slice` (`stale_limit=3`): consecutive non-empty pages banking ZERO new rows declare the slice complete — any silent clamp or overlap-saturated band on ANY source now costs a handful of calls, never a quota-day (also curbs Reed's band-overlap waste). Suite **425 passed**. Relaunched 02:21; acid test passed live: adzuna distinct 5,253 → **6,586 within minutes**, walking band £25,001–£28,125. (tests: 425 passed + 10 skipped, exit 0)
- 2026-07-25 00:02 BST — AGGREGATOR RUN RESUMED on the founder's word ("start the run"): wrapper relaunched detached just past midnight — fresh full quota day (950 Reed + 240 Adzuna); walking immediately (band £24,788–24,793 completed within the first minute). Adzuna's final ~12.5k ads land today; Reed bands continue.
- 2026-07-24 08:33 BST — AGGREGATOR RUN STOPPED by the founder ("stop it for now"): wrapper + script killed, stop file cleared, no processes left. State at stop: 27,621 distinct ads (27,617 UK) · 3,638 matched at 352 sponsors · Reed 950/950 today (napping anyway) · Adzuna 160/240, ~32k/44.5k. Resume anytime: relaunch `ops/run-aggregator.sh`; cursors exact; Adzuna has ~80 calls left today, Reed resumes at midnight quota.
- 2026-07-24 08:25 BST — Overnight Supabase-pooler flakiness (several resolved addresses timing out, incl. an IPv6 route) rode through on retries/relaunches; one morning cycle sat stuck in psycopg's dead-address walk while fresh connections succeeded, so the script (only) was killed — wrapper relaunched it and it connected first try. Zero loss (per-page commits). Overnight production: Reed spent its full 950 (~95k band-pages), Adzuna 160/240; totals 27,621 distinct ads · 3,638 matched at 352 sponsors.
- 2026-07-24 00:36 BST — Drip wrapper restarted to skip a thawed nap: the laptop was closed 19:18→~00:20 (wrapper froze mid-nap and survived — the freeze-resume design proven live over a 5-hour lid-close); rather than wait out the frozen countdown's remaining minutes, killed + relaunched on the founder's "start the run". Walking immediately on the 24th's fresh quota (Reed band £21,876–£23,438, +100 ads/page). Zero loss.
- 2026-07-23 17:41 BST — AGGREGATOR RUN RESUMED on the founder's word ("start the run"): wrapper relaunched detached, cursors resumed exactly; both day-quotas already spent (07:xx), so the drip correctly reported quota_exhausted per slice with zero API calls and entered its 30-min nap loop — fresh walking begins automatically at the 00:00 ledger reset.
- 2026-07-23 08:31 BST — AGGREGATOR RUN STOPPED by the founder ("stop the run I will be resuming it soon") via `.aggregator-stop`; wrapper exited cleanly from its quota-nap, stop file cleared, no processes left. State at stop: 23,250 distinct ads (23,246 UK) · 3,022 matched at 332 sponsors (311 of them board-less — pure long-tail gain) · Reed bands ~77k walked (band overlap slows distinct coverage; full pass now estimated at several more days, not one — logged honestly) · Adzuna 24,000/~44,537 · both day-quotas spent ~07:xx · harvest hints still 0 (watch item: Reed detail-API enhancement if it stays 0). Resume = relaunch `ops/run-aggregator.sh`; cursors exact.
- 2026-07-23 00:01 BST — REED 10K-WALL FIX SHIPPED + RUN RELAUNCHED (founder: "do the fix and keep the run going"). Live discovery ~23:30: Reed hard-walls at resultsToSkip=10,000 (HTTP 500, confirmed by paired probes at skip 9,900 vs 10,000); the runner's source_error guard correctly refused to fake-complete the pass (5 clean refusals, zero corruption). Fix: **salary-band partitioning** (`discover/agg_partition.py`) — adaptive binary splitting via 1-result count calls (probes showed salary filters cover 99.93% of inventory; open top band catches the rest), plan persisted as cursor rows, legacy `reed|` cursor retired, `page_reed` gains salary params, per-source outcomes combined via `combine_outcomes`. **Second live catch:** first plan hit the 64-leaf cap and silently DROPPED £25,042–£200,000 (sub-£25k clustering ate every leaf; caught by a contiguity SQL check minutes after relaunch) → planner rewritten "coverage is sacred" (cap flushes remaining ranges as oversized leaves, never drops; max_bands 256) + regression test pinning no-drop under clustered density. Re-planned live: **258 bands, £0→£200,000 contiguous, 0 gaps** (SQL-verified). Run relaunched 23:58 BST, walking. (tests: 421 passed + 10 skipped, exit 0)
- 2026-07-22 22:28 BST — AGGREGATOR RUN LAUNCHED on the founder's word ("start the run"): `nohup ops/run-aggregator.sh & disown`, wrapper PID detached to PPID 1 (Claude-independent), log `ops/aggregator-logs/agg-20260722T212821Z.log`. Verified live within the first minute: Reed resuming from page 2 (+100 ads/page, provider total ~92,945), 330 ads stored · 330 UK · 16 at 8 matched sponsors, quota ledger 3+1 calls. Expected shape: Reed full pass ≈ day one; Adzuna it-jobs ≈ 4 days at 240/day; drip self-stops on "pass complete (all slices)"; founder stops via `.aggregator-stop` / pkill.
- 2026-07-22 22:22 BST — MAXIMUM-COVERAGE BUILD SHIPPED (founder order: "build what is necessary… full data of all the jobs of all the companies… maximum coverage of real data… works for any third person"). RED-first throughout; **suite 402 → 416 passed, 0 failures**. (1) **Plan 0007 task 1b DONE** — keep-all reaches the pipeline: migration **0035** `role_listings.is_local + source` (backfilled from URL domains), `upsert_jobs` stores every job labelled, `fetch_jobs.py` filter deleted, `workday.py` stores non-UK postings shallow (no detail call — call bound unchanged) and keeps detail-relocated jobs, extractor picker narrowed to `is_local` (Gemini can never bill a foreign JD). (2) **Aggregator machine BUILT** — migration **0036** (`aggregator_ads` keep-all raw layer · `aggregator_cursor` resume · `api_quota_ledger` daily caps), broad-sweep pagers (Reed full-inventory `keywords=None`; Adzuna category browse), `agg_store`/`agg_match` (register-is-the-filter label pass, one lookup per distinct employer)/`agg_sweep` (quota-refusing, cursor-resumable, per-page commits, source_error ≠ pass_complete)/`token_harvest` (ad links → `parse_ats_url` → census board hints, `probe_outcome→NULL` re-queue) + **probe hint-first** (`probe_org` verifies harvested tokens with one call before slug guessing), `scripts/agg_sweep.py` (person-agnostic slices via flags, `--status`), `ops/run-aggregator.sh` (detached, `.aggregator-stop`, quota-sleep, Claude-independent). (3) **Live smoke (2 API calls): PROVEN** — Reed total **~92,925 ads** (full pass < 1 day of quota!), Adzuna it-jobs **~44,562**; 150 ads stored, **8 matched to 5 licensed sponsors on page 1**, cursors at page 2, quota ledgered 1+1; locality label corrected for country-scoped sources (bare UK towns local unless foreign-marked; 103 smoke rows relabelled → 150/150). **NO RUN STARTED** — founder starts it. (tests: 416 passed + 10 skipped, exit 0)
- 2026-07-22 21:47 BST — ADZUNA_APP_ID + ADZUNA_APP_KEY set in `.env` by founder and live-verified (5/5 results, 'machine learning engineer · London'; `adzuna_ready` true; salary data flowing — Sky/Kingfisher/Just Eat in the first pull). **Both aggregator keys now live → plan 0007 tasks 2–5 fully unblocked**; build order stands: dedupe (task 3) before any ad storage. Still no run started.
- 2026-07-22 21:38 BST — REED_API_KEY set in `.env` by founder and live-verified (5/5 results on a one-shot 'machine learning engineer · London' search; `reed_ready` true; engine client works unchanged). Adzuna still pending (2 values). No run started — aggregator lane stays gated behind cross-source dedupe (0007 task 3) per the 2026-07-22 design decision.
- 2026-07-22 21:26 BST — PASS 2 CENSUS COMPLETE (wrapper self-stopped 20:40 BST — evidence: `ops/sweep-logs/sweep-20260722T173825Z.log` mtime + closing lines "batch done: 0 picked" → "software lot fully probed — nothing left. Done."). Final DB truth: software lot fully probed · **260 live job boards** (259 at register-matched sponsors + 1 not_found org) · **5,144 census jobs stored** (1,196 UK · 288 title-matched · 253 orgs · 4 ATS sources) · **0 errors** across all three detached segments (launch 07-20 19:43 · founder stop/resume 07-21 · completion 07-22; survived one macOS TCC prompt + one internet outage via wrapper worker auto-restart). Plan 0003 → ✅ Done; PROJECT-MEMORY handoff updated; CLAUDE.md header refreshed. Next: founder promotion review (`list_software_companies(with_boards_only=true)`), plan-0007 task 1b before the fetch run.
- 2026-07-21 23:37 BST — Plan 0008 logged (third-party product: friend's profile pathways local/hosted, MCP-for-second-user, email-nudge feature incl. reply tracking, build order). Plan-only per founder; no code touched; Pass-2 sweep running throughout. (see decision-log same time)
- 2026-07-20 19:43 BST — PASS 2 LAUNCHED: NEW `ops/run-sweep.sh` (batch 500 × 4 workers, nohup+disown+caffeinate, graceful `.sweep-stop` stop-file, auto-restart) over the 11,726-company software lot; smoke batch of 5 clean (0 errors). Public-readiness scrub shipped the same day: snapshot staged as a fresh single-commit repo at `../goal-a-engine-public` (README rewritten as landing page; ref/email/deadline/narrative scrubbed and grep-verified) — GitHub push deferred by the founder until after the run starts. (tests: 402, suite exit 0)
- 2026-07-20 18:41 BST — PASS 1 CENSUS COMPLETE, recorded (wrapper self-stopped **2026-07-17 00:34 BST** — evidence: `ops/classify-logs/classify-20260716T174159Z.log` mtime; 126,342/126,342 unique register orgs; 11,726 software companies; 0 errors) + KEEP-ALL-JOBS census build (founder rule 2026-07-16: "fetch all the jobs it finds; filter later"): migration 0034 `census_jobs.is_local` (pre-0034 rows backfilled true), `insert_census_jobs` gains `local_matcher` and stores EVERY fetched job labelled title_match + is_local (matchers never filter), `probe_org` keeps locals first when the 500 cap bites, `local_jobs_seen` still counts locals only. RED-first (4 failing → green); pipeline-layer + Workday halves deferred to plan 0007 task 1b. (tests: 402 collected, suite exit 0)
- 2026-07-16 17:41 UTC → 07-17 00:34 BST — [backfilled 2026-07-20] Pass 1 final top-up batch ran and the wrapper self-stopped: "register fully classified — nothing left. Done."
- 2026-07-14/15 — [backfilled 2026-07-20] Pass 1 founder stop/relaunch cycles (evidence: `classify-20260714T173852Z.log`, `classify-20260715T184832Z.log` — relaunches 17:38 UTC and 18:48 UTC); per-company commits resumed exactly each time. One overnight connection drop self-recovered (wrapper crash-loop until DNS returned).
- 2026-07-14 — [backfilled 2026-07-20; work by the parallel SIC session] Migrations 0031–0033 applied (sic_codes table, v_sponsor_industry view, RLS hardening) + the `plans/` folder created with its multi-session conventions. That session logged in `plans/0002` but not here — backfilled so the audit trail is complete.
- 2026-07-13/14 (committed 2026-07-14 21:28 BST) — PIPELINE-VISION INSERTION (founder-directed, built mid-census with zero hot-file edits; see decision-log 2026-07-13 + docs/vision-pipeline.md): FIRST LOCAL COMMITS (hold lifted for local only) — snapshot dbb0e8f = Phases 1–7.5 rollback point. NEW src/discover/probe_pick.py (Pass-2 software-first picker: probe_outcome NULL + registry-matched + SOFTWARE_SIC, active first; run_software_sweep + run_software_sweep_parallel — N workers, one connection each, per-org commit kept); scripts/sweep.py gains --software-only [--workers N]; NEW src/discover/promote.py (promote_from_census: the founder-triggered, audited census→pipeline bridge — guards not_found/already_tracked/no_board, copies the board, register-only confidence); NEW src/discover/census_queries.py (list_software_companies, token-free); NEW src/analysis/job_gap.py (fetch_job_gap: per-role have/missing/coverage). MCP 19→24: run_classification + classify_status (Pass-1 switches, detached + scoreboard), list_software_companies, get_job_gap, promote_company; run_sweep gains software_only/workers. docs/dev.md updated (24 tools + Pass-2 runbook); docs/vision-pipeline.md maps the founder's flow end-to-end. Census untouched and running throughout. (tests: 400 collected, suite exit 0)
- 2026-07-12 14:22 BST — DECISION logged (see decision-log): Notion filing moves OUT of the engine to Claude's own Notion connector. Engine writes the apply queue to its DB (source of truth, headless); Claude reads via get_apply_queue / its Supabase connector and mirrors to Notion on demand or on a schedule; application status source-of-truth moves to the DB (mark_applied replaces the Notion→engine sync). Retires src/notion/* + the Notion half of cv/filing.py + NOTION_* config when the filing stage is next touched (Phase 8); CV generation stays engine-side. No code changed yet (census running); no NOTION_TOKEN needed from founder.
- 2026-07-12 13:10 BST — Pass 1 classification RESUMED after the overnight stop, running unattended + detached: new ops/run-classify.sh (auto-restart wrapper; per-company commit = exact resume after any transient death) launched under `caffeinate -i` so the Mac classifies with the screen off / Claude Code closed (not a session child → not in the task panel, by design). This session 3,181 → 6,051 of 126,342 (2,852 software, 0 errors, ~120k remaining; ~2,290/hr, ETA ~2 days). Stop with `pkill -f classify_sponsors` (no data lost); relaunch `bash ops/run-classify.sh` to resume.
- 2026-07-12 03:33 BST — Census re-architected into TWO PASSES (founder-directed): Pass 1 = classify. New src/discover/classify.py (pick_classify_batch + run_classify) maps Companies House SIC industry code + status + age onto every sponsor's census card — NO job-probing; reuses companies_house.enrich_org; own .classify.lock; per-company commit + error isolation. census_store gains ensure_census_card + classify_status_counts; SOFTWARE_SIC set defined (62012/62020/63110/…). scripts/classify_sponsors.py runner. No schema change (reuses 0030 registry_* columns). Live-verified: 23/25 matched, correctly separates real software (62020) from name-lookalike wholesale (46690)/financial (64999). Launched over full register (~1.5–2 days). Pass 2 (probe software-only) DEFERRED until Pass 1 completes. (tests: 366 offline + 10 DB)
- 2026-07-11 22:40 BST — Phase 7.5 follow-on (founder-directed): pick_batch now orders SOFTWARE-LIKELY names first (TECH_NAME_PATTERN regex over org_name_norm — software/tech/digital/data/ai/cloud/systems/fintech… with word-boundaries on short tokens) ahead of skilled-worker/A-rated/id. Ordering only, never a filter — every org is still probed, but tech sponsors surface first instead of after ~100k care homes. Test-first (existing ordering test stays green); live-verified the next 15 picks are all tech firms. Census relaunched in software-first 2000-org batches. (tests: 352 offline + 10 DB = 362)
- 2026-07-11 21:37 BST — SESSION CLOSE: Phase 7.5 (The Census Sweep — founder-directed insertion) COMPLETE. All 7 tasks, tests 351 offline + 10 DB-gated = 361, all green under RUN_DB_TESTS=1 (was 296+9). Migration 0030 sponsor_census+census_jobs applied + mirrored. MCP tool set 17→19 (run_sweep detached, sweep_status). Architecture v2 revised (machine principle + PHASE 7.5 card); CLAUDE.md rebuilt via /builder-md. LIVE-VERIFIED: 61 census cards; Synthesia/Quantexa boards found with 32 real-link jobs + correct title matches; blast radius held (target_companies 79, review_items 740 unchanged); resumability + lock + detached MCP trigger all proven. Two live finds fixed: scripts/discover.py package-shadowing bug (renamed discover_companies.py) and the candidate_tokens legal-suffix recall gap. Commits STILL held. FOUNDER ACTION: free Companies House key → COMPANIES_HOUSE_API_KEY in .env (blank = probe-only). Phase 7.5 CLAUDE.md archived; Phase 8 (Going Live) CLAUDE.md re-staged. Do NOT begin Phase 8 — awaiting founder.
- 2026-07-11 21:20 BST — Phase 7.5, Task 7b (live-smoke finds): scripts/discover.py renamed discover_companies.py (the script shadowed the src/discover package on sys.path — the Phase 6 discover stage would have crashed on its first live run); candidate_tokens now tries legal-suffix-stripped slug variants (strip_legal_suffixes moved to normalise/text.py, shared by sponsor_match + ats + companies_house) — Synthesia Limited / Quantexa Ltd now card board_found with real Ashby links (tests: 351 offline + 10 DB)
- 2026-07-11 19:15 BST — Phase 7.5, Task 7: docs updated (dev.md 14→19 tools + missing rows + census runbook); LIVE SMOKE: two 25-org batches (resumable — second picked the NEXT 25, zero re-probes), a second concurrent sweep declines on .sweep.lock, MCP run_sweep spawns detached (log under ops/sweep-logs/, secret-free audit row), sweep_status live at 126,342 unique orgs; blast radius verified — target_companies/review_items counts unchanged
- 2026-07-11 18:50 BST — Phase 7.5, Task 6: src/mcp_server/census_tools.py — run_sweep(batch_size) spawns scripts/sweep.py DETACHED (start_new_session, log → ops/sweep-logs/, audited) so an hours-long sweep never blocks a chat call; sweep_status read-only (no audit — reads never audit); registered in server.py; toolset contract 17→19 (tests: 348 offline + 10 DB)
- 2026-07-11 18:35 BST — Phase 7.5, Task 5: scripts/sweep.py thin runner — its own .sweep.lock (sweep + daily pipeline coexist; two sweeps cannot), --batch (default 2000) / --pause / --retry-errors / --probe-only, [sweep] stderr progress every 25 orgs + census totals line, per-item errors never exit non-zero
- 2026-07-11 18:20 BST — Phase 7.5, Task 4: src/discover/companies_house.py — the UK national-registry plug-in (the sweep imports it as `registry`: the one-line country seam); contract verified live from the CH developer docs (Basic auth key-as-username blank password; 600 req/5 min → 0.6 s pace + 429 backoff); deterministic match: exact norm / unique suffix-stripped / single-ACTIVE among dissolved namesakes, ambiguity recorded never guessed; COMPANIES_HOUSE_API_KEY + ch_ready in config + .env.example (blank = probe-only)
- 2026-07-11 18:00 BST — Phase 7.5, Task 3: probe_org + run_sweep in src/discover/sweep.py — classify → fetch ONCE → local filter (behind the _is_local seam) → census jobs capped 500/org; fetch failure keeps board_found with local_jobs_seen NULL + error noted; per-org error isolation and per-org COMMIT (exact crash-resume); swappable _sleep pacing; THE blast-radius pin test: the sweep never writes target_companies or review_items
- 2026-07-11 17:40 BST — Phase 7.5, Task 2: pick_batch + load_tracked_orgs in src/discover/sweep.py — next-N picker groups register rows by org_name_norm (skilled-worker/A-rated first, NOT EXISTS anti-join vs sponsor_census, bound limit, retry-errors variant); the tracked map keys BOTH python-normed company names and register-linked norms so already-tracked orgs get marked without re-probing and "remaining" converges
- 2026-07-11 17:20 BST — Phase 7.5, Task 1: migration 0030 sponsor_census + census_jobs (country-neutral columns: registry_*, industry_codes, local_jobs_seen, country default 'uk'; census_jobs deliberately stores NO JD body; RLS on; advisors clean — only the expected INFO) applied via Supabase MCP + mirrored; src/discover/census_store.py — every census write + the status counts (upsert by org_name_norm; shared dedupe_key with ON CONFLICT DO NOTHING), 11 offline + 1 scratch-schema DB test
- 2026-07-11 04:27 BST — SESSION CLOSE: Phase 7 (CV Maker & Notion Filing) COMPLETE. All 7 tasks, tests 296 offline + 9 DB-gated = 305 (was 244+8). Migration 0029 cv_blocks applied + mirrored; python-docx added. MCP tool set 16→17 (generate_cv). Commits STILL held. FOUNDER ACTIONS before live CVs: seed cv_blocks with confirmed CV facts; create a Notion integration + set NOTION_TOKEN/PARENT/DATABASE; a CV host for attachments comes in Phase 8. Phase 7 CLAUDE.md archived; Phase 8 (Going Live) CLAUDE.md written + swapped in. Do NOT begin Phase 8 — awaiting founder.
- 2026-07-11 04:20 BST — Phase 7, Task 7: wired into the daily loop — `file` stage (scripts/file_applications.py) after eval/before nudge via src/cv/filing.run_filing_stage (CV + idempotent Notion card per gate-passing listing, ranked; sync 'Applied' back); nudge gains a Notion board-link footer; generate_cv(role_id, emphasis) MCP tool (17th) re-tailors on request; stage skips cleanly without Notion, isolates a bad listing (tests: 296 offline + 9 DB)
- 2026-07-11 04:05 BST — Phase 7, Task 6: src/notion — direct REST client (headless, not the Claude MCP) + Applications tracker; one idempotent card per listing keyed on a Role ID number property (query→update/create), external-URL CV property, 'Applied' sync-back read; mocked-API tests; NOTION_* in .env (blank=skip)
- 2026-07-11 03:58 BST — Phase 7, Task 5: src/cv/render.py — single-column ATS-safe .docx via python-docx (no tables/columns/images; real headings + bullet lists); golden-structure test; verified by read-back as a clean ATS-parseable CV
- 2026-07-11 03:52 BST — Phase 7, Task 4: src/cv/truth.py — the truth gate (reuses read.eval grounding); every number must be grounded + ≥75% content-word coverage, else the bullet falls back to the verbatim fact; catches invented numbers/entities, tolerates synonyms
- 2026-07-11 03:47 BST — Phase 7, Task 3: src/cv/phrase.py — AI spot #3, Gemini rephrases verified facts only (reuses read.gemini client+retry, temp 0, 'add nothing'); falls back to the un-rephrased fact on no-key/API-error/missing-bullet; mocked-client tests
- 2026-07-11 03:42 BST — Phase 7, Task 2: src/cv/assemble.py — pure deterministic block selection by skill overlap with the listing (ties on sort_hint then block_id); two listings provably yield different CVs from one fact base
- 2026-07-11 03:35 BST — Phase 7, Task 1: migration 0029 cv_blocks (verified career facts; confirmed gate; fact_text grounding source; skill_norms[] matching role_skills; owner-scoped, RLS no-policy) applied via Supabase MCP + mirrored; tested loader src/cv/blocks.py (5 tests incl. seeded-slice DB)
- 2026-07-11 03:53 BST — SESSION CLOSE: Phase 6 (Discovery Engine) COMPLETE. All 7 tasks, tests 244 offline + 8 DB-gated = 252 (was 186+6). No DDL (discovery rides existing tables; new kinds/ats_type are data). MCP tool set 14→16. Commits STILL held. FOUNDER ACTIONS before live discovery: seed `industry_hint` rows in my_constraints; add free Adzuna + Reed keys to .env. Phase 6 CLAUDE.md archived; Phase 7 (CV Maker & Notion Filing) CLAUDE.md written + swapped in. Do NOT begin Phase 7 — awaiting founder.
- 2026-07-11 03:50 BST — Phase 6, Task 7: discovery joins the daily schedule — `discover` runs FIRST in scripts/run.py (same-run fetch of today's finds); src/discover/daily.py orchestrates register + adzuna + reed under per-source caps (register 25, aggregators 50/query, onboard 15), one report line per source into pipeline_runs, failing source isolated; scripts/discover.py thin runner (tests: 244 offline + 8 DB)
- 2026-07-11 03:40 BST — Phase 6, Task 6: src/discover/sponsor_match.py — aggregator employers matched to the register; confident on exact-norm OR unique legal-suffix-normalised match, ambiguous/partial → `sponsor_match` review flag (never a silent guess), no hit → confident negative; 9 tricky-name-pair tests
- 2026-07-11 03:30 BST — Phase 6, Task 5: src/discover/aggregators.py — Adzuna (app_id/app_key params) + Reed (HTTP Basic, key as username) clients, keys in .env (config Settings + .env.example; blank = source skipped), results normalised to the standard Job; fixtures from the official docs; 7 tests
- 2026-07-11 03:15 BST — Phase 6, Task 4: src/fetch/workday.py — Workday CXS board adapter; real shapes captured live from NVIDIA/Adobe/Salesforce, fixtures recorded; ats_token carries the careers URL, fetch_company dispatches workday (lazy import) → standard pipeline; UK-filter then bounded detail-fetch; 8 tests
- 2026-07-11 03:00 BST — Phase 6, Task 3: discover_company + classify_from_url MCP tools (thin wrappers; logic in src/discover/company.py); by-name probe → onboard-or-evidence, URL → verify board → onboard; sponsor-register verdict attached both ways; tool set 14→16, invariant green
- 2026-07-11 02:58 BST — Phase 6, Task 2: src/discover/onboarding.py — candidate → ATS classifier → board hit joins the fetch list (ats_type+ats_token, register-only), miss → `company_onboard` review flag (idempotent, probe evidence); add_flag added to review.py; every write audited; both paths DB-verified
- 2026-07-11 02:56 BST — Phase 6, Task 1: src/discover/register.py — walk licensed_sponsors (A-rated Skilled Worker) for candidates not yet in target_companies, owner-scoped, region/industry hints from my_constraints (no personal values in code); org_name_norm==norm() verified across all 141,956 rows; 11 tests incl. seeded-slice DB integration
- 2026-07-11 02:51 BST — SESSION CLOSE: Phase 5 COMPLETE + DB-verified. FastMCP stdio server, 14 tools, thin skin over the engine (a test proves no engine file imports it). review_items(0027)+mcp_audit(0028) applied via Supabase MCP + mirrored; 740 low-conf synonyms seeded; advisors clean. Read tools + run_pipeline dry-run + review list + audit row all live-verified. Phase 5 CLAUDE.md archived; Phase 6 CLAUDE.md written + swapped in. Commits STILL held. Next session: build Phase 6 (Discovery Engine). Tests: 186 offline + 6 DB-gated.
- 2026-07-11 02:50 BST — Phase 5, Task 7: ops/claude-mcp-config.json (stdio registration, absolute paths, cwd-independent) + docs/dev.md MCP section (setup + 14-tool table); `python -m mcp_server.server` verified to start the stdio server from any cwd (tests: 186 offline + 6 DB)
- 2026-07-11 02:40 BST — Phase 5, Task 6: full-toolset contract test (exactly 14 tools, each with a description) + independence-invariant test (no engine file imports src/mcp_server — killing the skin can't touch the daily loop); every tool already driven offline via the in-process fastmcp.Client
- 2026-07-11 02:25 BST — Phase 5, Task 5: mcp_audit table (migration 0028) + src/audit.record; every action + resolve_review_flag writes an arg/result summary (never a secret) in the action's own transaction; no-op set_criteria/failed resolve write nothing; live-verified via run_pipeline dry-run
- 2026-07-11 02:05 BST — Phase 5, Task 4: review tools (list_review_flags/resolve_review_flag) over src/review; review_items table (migration 0027, RLS) seeded from 740 confidence='low' skill_synonyms (CLAUDE.md said 74 — stale count, flagged); live list verified, advisors clean
- 2026-07-11 01:30 BST — Phase 5, Task 3: 6 action tools (mark_applied/snooze_listing/set_criteria/add_target_company/run_pipeline/send_test_nudge) as thin skins over tested writes (applyqueue, criteria/writer, pipeline/trigger, notify.send_test); run_pipeline shells the SAME run.py, dry-run live-verified (~63 would nudge); jobqueue.py untouched (Rule 2)
- 2026-07-11 00:55 BST — Phase 5, Task 2: 6 read tools (get_apply_queue/get_job/get_job_history/get_skill_gaps/get_run_report/get_criteria) over tested src read fns; curated columns exclude every secret; all 6 live-verified read-only on GOAL A (0 secrets leaked)
- 2026-07-11 00:30 BST — Phase 5, Task 1: claude-api skill loaded; fastmcp>=3.4,<4 added (requirements.lock 35→82); src/mcp_server FastMCP-over-stdio server + tool-registry pattern; src/mcp→src/mcp_server rename resolved a package-shadow that broke fastmcp (founder-approved)
- 2026-07-11 00:02 BST — Doc consolidation: retired docs/handoffs/ + PROJECT-MEMORY.md; CLAUDE.md End-of-Phase now self-contained (4 steps). docs/ = architecture/, decision-log, progress-log, claude-md-archive/ (+ PRD, dev.md kept). Single memory file: GOAL/SESSION-MEMORY.md. Phase 5 CLAUDE.md unchanged and ready. See decision-log 00:02 for why.
- 2026-07-10 23:00 BST — SESSION CLOSE: Phase 4 complete + verified by first full automatic run (all stages ok, 0 failures; fetch 731 UK jobs, Gemini read 223 roles/2060 skills, 232 synonyms, salary 91/730, 729 deadlines, eval passed 0.60 gate; 514 history events, 100-role queue, 45 SOC-resolved). Phase 5 CLAUDE.md written + swapped in; Phase 4 CLAUDE.md archived. All logs + memory current. Commits STILL held by founder instruction. Next session: build Phase 5 (MCP server) per the new CLAUDE.md.
- 2026-07-10 22:25 BST — Phase 4, Tasks 1–6: pipeline orchestrator (pure, stub-tested; failure isolation) + pipeline_runs report table (0026) + run.py as full daily entrypoint (fetch→read→synonyms→salary→deadlines→eval --min 0.60→nudge); src/notify (ntfy client — failures return False, never sink a run; failure nudges so broken runs are never silent); nudge policy (High fit + positive sponsor signal, one digest, nudged_at stamps only after successful send); flock run lock (tested); launchd plist validated (founder loads it — his spend switch); --dry-run previewed live: 57 roles would nudge. Tests: 122 (116 offline + 6 DB)
- 2026-07-10 22:08 BST — Phase 3, Task 6: v_apply_queue appends deadline/deadline_source/age_days/last_changed_at (migration 0025, lateral over listing_events); jobqueue CLI shows "12d old | changed 09 Jul | apply by (est.) 19 Jul"; integration tests extended to 6 (tests: 106 total, all passing incl. DB)
- 2026-07-10 22:07 BST — Phase 3, Task 5: description-change → surgical extracted_at reset (that listing only); pinned: salary-only change never re-bills; retitle = new identity by dedupe design (pinned deliberately)
- 2026-07-10 22:06 BST — Phase 3, Task 4: src/history/deadline.py — stated-deadline extraction (phrase-anchored, 4 date formats, never inferred from bare dates) + labelled estimate (first_seen + profile window); scripts/enrich_deadlines.py ran live: 754 open roles → 1 stated, 753 estimated (21d window)
- 2026-07-10 22:05 BST — Phase 3, Tasks 2+3: src/history (fingerprint, field diffs — description as lengths only; plan_events: appeared/changed/reopened-with-diff, never reopened+changed pairs); wired into fetch per-company transaction (upsert carries fingerprint, close_vanished returns ids, closed events recorded); reopened pinned by tests
- 2026-07-10 22:03 BST — Phase 3, Task 1: listing_events table + content_fingerprint + deadline_source + profiles.apply_window_days (migration 0024)
- 2026-07-10 22:01 BST — GA-001 closed: rotated keys live-verified (DB + Gemini); 11 unread roles extracted live via Gemini — 0 failures, read-once stamped zero-skill roles (the old leak's exact victims), resolver confirmed 'Customer service managers'→4143 and refused casual titles
- 2026-07-10 21:52 BST — Phase 2, Task 7: clears_wall/soc_tier dropped (verified 0/754 filled first; migration 0023). Suite: 81 offline + 5 DB integration = 86 passing, offline in 0.79s (fixed a real-sleep leak my retry change caused in two old ats tests)
- 2026-07-10 21:48 BST — Phase 2, Tasks 5+6: v_apply_queue rebuilt (migration 0022) — per-SOC wall (70% new-entrant band, flat fallback, wall_basis transparency column), owner-scoped thresholds, UK guard mirrors hardened is_uk, owner_id surfaced; verified live (84 rows unchanged). First-ever queue tests: 5 opt-in integration tests (RUN_DB_TESTS=1) against a scratch schema built from the real migration DDL — ranking, sponsor precedence, wall verdicts, UK guard all pinned
- 2026-07-10 21:45 BST — Phase 2, Task 4: SOC resolver (src/analysis/occupations.py — official names + related_job_titles, ambiguity→None, never guesses); soc_hint evidence column added, legacy free-text soc_code values moved there (580 hints: 34 resolved, 546 honestly NULL); FK role_listings.soc_code→skilled_worker_occupations (migrations 0020+0021); persist_reading stores raw hint, only resolver-confirmed codes
- 2026-07-10 21:40 BST — Phase 2, Task 3: soc_going_rates seeded — 17 tech-relevant codes from gov.uk Appendix Skilled Occupations Table 1 (page updated 1 Jul 2026, fetched live); 2134=£54,700; partial seed by design, unknown codes fall back (migration 0019). NOTE: skilled_worker_occupations holds no rate data — v1's assumption was wrong, gov.uk was fetched directly
- 2026-07-10 21:37 BST — Phase 2, Task 2: src/criteria (loader + role matcher, tested); fetch_jobs ROLE_RE deleted — role patterns come from target_roles per owner, generic samples for offline dry-run; grep-proof: no personal values in src/ or scripts/
- 2026-07-10 21:34 BST — Phase 2, Task 1: profiles table + owner_id (FK, backfilled 100%: 22 skills/14 constraints/39 roles/79 companies) with single-user DEFAULT (removed in Phase 9); singleton-threshold index now per-owner (migration 0018)
- 2026-07-10 21:23 BST — Phase 1, Task 11: deps pinned in pyproject.toml (+dev group) with requirements.lock (35 pkgs); requirements.txt removed, docs updated; upsert/skill-insert/salary/synonym writes batched via executemany; migration 0017 partial unique index on my_constraints singleton kinds (tests: 70 passing)
- 2026-07-10 21:20 BST — Phase 1, Task 10: single shared normaliser src/normalise/text.py with identity-enforcing test; duplicate norms deleted (extract_skills, feeds normalize_title); dead salary classify()+thresholds and config.general_salary_threshold() deleted; .env.example cleaned; golden dedupe_key test pinned (tests: 69 passing)
- 2026-07-10 21:18 BST — Phase 1, Task 9: src/persist/fetch_rules gains upsert_jobs/close_vanished/finish_run with fake-cursor tests; fetch_jobs.py now a thin runner (tests: 67 passing)
- 2026-07-10 21:15 BST — Phase 1, Task 8: read-once fixed — extracted_at column (migration 0016, 733/754 backfilled, 11 genuinely unread), zero-skill readings stamped, no-clobber COALESCE preserved; src/persist/extract_rules.py with fake-cursor tests (tests: 64 passing)
- 2026-07-10 21:14 BST — Phase 1, Task 7: canonicalize_batches() persists each Gemini batch as it returns; one failed batch no longer discards paid work (tests: 61 passing)
- 2026-07-10 21:13 BST — Phase 1, Task 6: per-company commits in fetch run; two-consecutive-empty-runs rule (src/persist/fetch_rules.fetch_outcome) before job-rot may close a company's listings (tests: 59 passing)
- 2026-07-10 21:12 BST — Phase 1, Task 5: retry-with-backoff at all three outbound seams (feeds._get, ats._get_json, gemini.generate_with_retry shared by both AI spots); Gemini client HTTP timeout; 404 never retried (tests: 56 passing)
- 2026-07-10 21:11 BST — Phase 1, Task 4: is_uk() hardened (strong markers vs ambiguous city names + foreign disqualifiers + authoritative country codes); 754 stored listings scanned, 0 false rows (tests: 48 passing)
- 2026-07-10 21:10 BST — Phase 1, Task 3: docs/PRD.md written; dev.md made honest (dead env var + src/queue/ refs removed, pipeline run commands documented); README AI-spots claim corrected
- 2026-07-10 21:09 BST — Phase 1, Task 2: LICENSE (MIT) + pyproject licence/author metadata + README licence section; .gitignore covers .env.* (with !.env.example) and PROJECT-MEMORY.md
- 2026-07-10 20:54 BST — Phase 1 prep: CLAUDE.md archive folder + decision/progress logs created; Phase 1 task list registered; build awaiting rotation-gate confirmation (Task 0).
