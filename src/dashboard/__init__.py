"""The read-only Today page (Phase 7.8).

One local, token-protected screen: ready-to-apply vs needs-something (with
receipts), the honesty panel, company watch — and nothing else. The package
reads ONLY the three curated views (v_today, v_scorecard, v_health); the
complexity-hiding pin test enforces it. No writes, no JS, no framework.
"""
