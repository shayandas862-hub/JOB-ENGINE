"""Discovery — finding jobs the owner never pointed the engine at.

The register walk (register.py) is the unfair advantage: every candidate it
emits is already a UK licensed sponsor. Later modules onboard, probe boards,
and cross-check aggregator hits against the same register.
"""
