"""The pure math core (Phase 7.8).

Every number the product shows comes from one of these modules, and every
function returns its receipts alongside its result — no naked numbers. Pure
by rule: no DB, no AI, no network; callers fetch the data and choose the
constants.
"""
